# C1 — `style.css` 분할 + 빌드 번들링 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `api/static/css/style.css`(4,165줄)를 의미 단위 19개 파일로 분할한 뒤, `tools/build_css.py`가 런타임에는 단일 `style.css`로 번들링하도록 바꾼다. 렌더 결과는 분할 전후 완전 동일, HTTP 요청 수 변화 없음.

**Architecture:** `api/static/css/src/` 아래 `01_tokens.css` ~ `19_responsive.css` 로 섹션별 이동. 파일명 prefix로 로드 순서(= 원본 cascade) 보존. `00_legacy.css`(전환 기간 동안 원본 원본 잔여)가 섹션 이동에 따라 축소되다가 T10에서 삭제. 매 커밋마다 `python -m tools.build_css` 재실행하여 `style.css` 갱신 후 함께 커밋.

**Tech Stack:** Python 3.10+ (stdlib만), Vanilla CSS, Jinja2 템플릿 (무수정)

**Spec:** `docs/superpowers/specs/2026-04-20-c1-css-split-design.md`

---

## Task 1: CSS Parity baseline 캡처

분할 전후 선택자/속성 parity 검증용 baseline 생성.

**Files:**
- Create: `tools/c1_css_split/__init__.py` (빈 파일)
- Create: `tools/c1_css_split/capture_css.py`
- Create: `tools/c1_css_split/baseline.json` (스크립트 실행 산출물)

- [ ] **Step 1: `tools/c1_css_split/__init__.py` 생성 (빈 파일)**

Run:
```bash
mkdir -p tools/c1_css_split
touch tools/c1_css_split/__init__.py
```

- [ ] **Step 2: `tools/c1_css_split/capture_css.py` 작성**

Create `tools/c1_css_split/capture_css.py`:

```python
"""style.css의 선택자→속성 매핑을 JSON으로 캡처.

사용: python -m tools.c1_css_split.capture_css > tools/c1_css_split/baseline.json
비교: python -m tools.c1_css_split.capture_css | diff - tools/c1_css_split/baseline.json

주석·공백 무시. 규칙 순서는 리스트 position으로 보존.
@media, @keyframes 등 at-rule은 블록 전체를 단일 item으로 처리.
"""
import hashlib
import json
import re
import sys
from pathlib import Path


def parse_css(text: str) -> list[dict]:
    """단순 파서 — {selector, decls_hash, decl_count, position} 리스트 반환."""
    text = re.sub(r"/\*.*?\*/", "", text, flags=re.DOTALL)
    items: list[dict] = []
    pos = 0
    position = 0
    while pos < len(text):
        while pos < len(text) and text[pos].isspace():
            pos += 1
        if pos >= len(text):
            break
        depth = 0
        start = pos
        in_block = False
        while pos < len(text):
            c = text[pos]
            if c == "{":
                depth += 1
                in_block = True
            elif c == "}":
                depth -= 1
                if depth == 0:
                    pos += 1
                    break
            pos += 1
        if not in_block:
            break
        block = text[start:pos].strip()
        brace = block.find("{")
        selector = re.sub(r"\s+", " ", block[:brace].strip())
        decls = block[brace + 1 : -1]
        decl_map: dict[str, str] = {}
        for d in re.split(r";(?![^(]*\))", decls):
            d = d.strip()
            if not d or ":" not in d:
                continue
            name, _, value = d.partition(":")
            decl_map[name.strip().lower()] = re.sub(r"\s+", " ", value.strip())
        decls_hash = hashlib.sha256(
            json.dumps(sorted(decl_map.items()), ensure_ascii=False).encode()
        ).hexdigest()[:16]
        items.append(
            {
                "position": position,
                "selector": selector,
                "decls_hash": decls_hash,
                "decl_count": len(decl_map),
            }
        )
        position += 1
    return items


def main() -> int:
    path = Path("api/static/css/style.css")
    items = parse_css(path.read_text(encoding="utf-8"))
    json.dump(
        {"total_rules": len(items), "rules": items},
        sys.stdout,
        indent=2,
        ensure_ascii=False,
    )
    sys.stdout.write("\n")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: baseline.json 생성**

Run:
```bash
venv/Scripts/python -m tools.c1_css_split.capture_css > tools/c1_css_split/baseline.json
```

- [ ] **Step 4: baseline 검증**

Run:
```bash
venv/Scripts/python -c "
import json
d = json.load(open('tools/c1_css_split/baseline.json'))
print(f'total_rules = {d[\"total_rules\"]}')
print(f'first selector: {d[\"rules\"][0][\"selector\"][:60]}')
print(f'last selector: {d[\"rules\"][-1][\"selector\"][:60]}')
"
```

Expected: `total_rules = <N>` where N is in the range ~600-900 (style.css has ~4165 lines, ~118 comment sections, many rules per section). First selector should be `:root` or similar. No errors.

- [ ] **Step 5: 참고용 메트릭 기록**

Run:
```bash
grep -c "!important" api/static/css/style.css
wc -l api/static/css/style.css
```

Expected: 특정 수의 `!important` 개수(예: 15~40개). 4165줄.

이 두 값을 T11 메모에 기록하기 위해 **stderr로 출력하거나 메모장에 적어둘 것**. T10 완료 후 재확인.

- [ ] **Step 6: Commit**

```bash
git add tools/c1_css_split/
git commit -m "$(cat <<'EOF'
chore(css): C1 — baseline 캡처 스크립트 + 스냅샷

분할 전 style.css의 선택자·속성 해시 목록을 JSON으로 캡처. T3~T10의
매 커밋마다 이 baseline과 비교하여 CSS rule parity를 검증한다.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 빌드 스크립트 + src 스캐폴드 (00_legacy에 원본 전체 복사)

`tools/build_css.py` 추가 + `api/static/css/src/` 생성. 이 단계에서는 19개 파일은 빈 placeholder만 놓고 `00_legacy.css`에 원본 전체를 복사해서 빌드 결과가 기능적으로 원본과 동일하도록 보장.

**Files:**
- Create: `tools/build_css.py`
- Create: `api/static/css/src/00_legacy.css` (원본 전체 복사본)
- Create: `api/static/css/src/01_tokens.css` ~ `19_responsive.css` (19개 빈 placeholder)
- Create: `api/static/css/README.md`
- Create: `.gitattributes` (루트에 없으면 생성, 있으면 수정)
- Modify: `api/static/css/style.css` (빌드 결과로 치환 — 실제로 같은 내용, 헤더만 추가)

- [ ] **Step 1: `tools/build_css.py` 작성**

Create `tools/build_css.py`:

```python
"""src/*.css를 정렬된 순서로 합쳐 style.css 생성.

사용: python -m tools.build_css
"""
from pathlib import Path

SRC = Path("api/static/css/src")
OUT = Path("api/static/css/style.css")
HEADER = "/* AUTO-GENERATED — edit files in src/ and run `python -m tools.build_css` */\n\n"


def main() -> int:
    files = sorted(SRC.glob("*.css"))
    if not files:
        print(f"[css] no source files in {SRC}")
        return 1
    parts = [HEADER]
    for f in files:
        parts.append(f"/* ======== {f.name} ======== */\n")
        parts.append(f.read_text(encoding="utf-8").rstrip() + "\n")
        parts.append("\n")
    OUT.write_text("".join(parts), encoding="utf-8")
    total_src = sum(f.stat().st_size for f in files)
    print(f"[css] bundled {len(files)} files → {OUT} (src total: {total_src} bytes, out: {OUT.stat().st_size} bytes)")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: `api/static/css/src/` 디렉터리 + 빈 placeholder 생성**

Run:
```bash
mkdir -p api/static/css/src
for name in 01_tokens 02_base 03_layout 04_cards_grids 05_badges 06_tables_bars 07_modals 08_buttons_forms 09_proposals 10_themes 11_dashboard_hero 12_dashboard_rest 13_stock_analysis 14_chat 15_admin 16_inquiry 17_history 18_mobile_features 19_responsive; do
  echo "/* $name — populated in later tasks */" > "api/static/css/src/$name.css"
done
ls api/static/css/src/
```

Expected: 19 files listed.

- [ ] **Step 3: `00_legacy.css`에 원본 전체 복사**

Run:
```bash
cp api/static/css/style.css api/static/css/src/00_legacy.css
wc -l api/static/css/src/00_legacy.css
```

Expected: `4165 api/static/css/src/00_legacy.css` (원본과 동일 줄수).

- [ ] **Step 4: 첫 빌드 실행**

Run:
```bash
venv/Scripts/python -m tools.build_css
```

Expected: `[css] bundled 20 files → api/static/css/style.css (src total: <X> bytes, out: <Y> bytes)` where 20 = 00_legacy + 01~19.

- [ ] **Step 5: CSS parity 검증 — T2 단계 공백 diff**

Run:
```bash
venv/Scripts/python -m tools.c1_css_split.capture_css > tools/c1_css_split/after_t2.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('tools/c1_css_split/after_t2.json'))
assert base['total_rules'] == after['total_rules'], f'rule count changed: {base[\"total_rules\"]} -> {after[\"total_rules\"]}'
# 같은 순서, 같은 selector + decls_hash여야 함
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    if b['selector'] != a['selector'] or b['decls_hash'] != a['decls_hash']:
        print(f'mismatch at position {i}: base={b[\"selector\"][:60]!r} -> after={a[\"selector\"][:60]!r}')
        break
else:
    print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm tools/c1_css_split/after_t2.json
```

Expected: `parity OK — <N> rules identical` where N matches baseline count.

빈 placeholder 파일들에는 아직 규칙이 없으므로(주석만) 규칙 수 변화 없음. `00_legacy.css`가 원본 그대로이므로 parity 통과.

- [ ] **Step 6: API 서버 스모크 (CSS 로드 확인)**

Run:
```bash
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `api.main ok`

라이브 환경에서 가능하면 브라우저로 `http://localhost:8000/` 접속하여 레이아웃 정상 확인.

- [ ] **Step 7: `api/static/css/README.md` 작성**

Create `api/static/css/README.md`:

```markdown
# CSS 빌드 가이드

## 구조

이 디렉터리는 빌드된 결과물(`style.css`)과 소스 파일(`src/*.css`)로 구성됩니다.

- `style.css` — **빌드 결과물.** 직접 편집하지 마세요. 파일 상단에 `AUTO-GENERATED` 표기.
- `src/01_tokens.css` ~ `src/19_responsive.css` — 도메인별 분할된 소스 파일. 이곳에서 편집.
- `src/00_legacy.css` — **C1 리팩토링 진행 중에만 존재.** T10에서 삭제 예정.

## 빌드

```bash
python -m tools.build_css
```

`src/*.css`를 alphabetical 순서로 합쳐 `style.css`를 갱신합니다. 파일명의 숫자 prefix가 로드 순서(cascade)를 결정합니다.

## 파일 목록

| 파일 | 담당 |
|---|---|
| `01_tokens.css` | `:root` CSS 변수 (색상·여백·alias) |
| `02_base.css` | Reset & Base |
| `03_layout.css` | Sidebar, Header, Content, Notifications, User dropdown, Mobile menu button |
| `04_cards_grids.css` | Cards, Stat Grid, Market Summary/Insights |
| `05_badges.css` | Badges, Tier, Theme Type, Importance, Grade, Role/Status |
| `06_tables_bars.css` | Tables, Screener, Confidence/Allocation bars, Indicators, Sparkline |
| `07_modals.css` | Upgrade/Common/Confirm modals, Toast |
| `08_buttons_forms.css` | Button variants, Filters, Pagination, Empty state, External links |
| `09_proposals.css` | Proposal card/row/detail tabs, Sticky CTA, Tracking badges, Price target |
| `10_themes.css` | Scenario, Macro impact, Issue timeline, Theme 신뢰도 ring, Sector bubble |
| `11_dashboard_hero.css` | Tier1 Hero, KPI Strip, Insight cards, Yield curve, Market summary 접이식 |
| `12_dashboard_rest.css` | Signals, News, Top Picks, Track Record widget, Card locked |
| `13_stock_analysis.css` | Stock Analysis 페이지 전용 |
| `14_chat.css` | Theme Chat |
| `15_admin.css` | Admin, User Admin |
| `16_inquiry.css` | 고객 문의 게시판 |
| `17_history.css` | History Timeline |
| `18_mobile_features.css` | Bottom tabbar, Touch target, Safe area, Table stack, Ad slot, Disclaimer banner, Risk Temperature SVG |
| `19_responsive.css` | Responsive base, Tablet(UI-18), 모바일 전용 |

## 새 CSS 추가 방법

1. 적절한 파일에 규칙 추가 (없으면 새 파일 생성하되 기존 prefix 체계 따를 것)
2. `python -m tools.build_css` 실행
3. `src/*.css` + `style.css` 함께 커밋
```

- [ ] **Step 8: `.gitattributes` 설정**

Check if `.gitattributes` exists:
```bash
ls -la .gitattributes 2>/dev/null && cat .gitattributes || echo "does not exist"
```

If it does not exist, create `.gitattributes` in project root:
```
api/static/css/style.css linguist-generated
```

If it exists, append the line (check it's not already there):
```bash
grep -q "style.css linguist-generated" .gitattributes || echo "api/static/css/style.css linguist-generated" >> .gitattributes
```

- [ ] **Step 9: Commit**

```bash
git add tools/build_css.py api/static/css/src/ api/static/css/README.md api/static/css/style.css .gitattributes
git commit -m "$(cat <<'EOF'
chore(css): C1 — 빌드 스크립트 + src/ 스캐폴드 + 00_legacy.css

tools/build_css.py가 api/static/css/src/*.css를 alphabetical 순서로
합쳐 style.css를 생성한다. src/에 19개 빈 placeholder + 00_legacy.css
(원본 전체 복사본)를 두어 T3~T10에서 섹션을 점진적으로 이동할 수
있도록 준비. .gitattributes에 style.css linguist-generated 표시.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `01_tokens` + `02_base` + `03_layout` 이동

`00_legacy.css`의 상단 섹션 3개를 각 파일로 이동.

**Files:**
- Modify: `api/static/css/src/01_tokens.css` (populate)
- Modify: `api/static/css/src/02_base.css` (populate)
- Modify: `api/static/css/src/03_layout.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove the moved sections)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상 식별**

Read `api/static/css/src/00_legacy.css`와 `docs/superpowers/specs/2026-04-20-c1-css-split-design.md`의 부록 A를 참조. 이번 이동할 섹션:

- `01_tokens.css` ← `:root { ... }` 블록 (대략 legacy 4~30줄 근처)
- `02_base.css` ← 그 외 `/* ===== Reset & Base ===== */` 섹션 내용(대략 1줄 주석부터 40줄까지 중 `:root` 제외)
- `03_layout.css` ← 다음 섹션들:
  - `/* ===== Sidebar ===== */` (42~134줄)
  - `/* 그룹 타이틀 — IA 재그룹화(UI-10) */` (135~167줄)
  - `/* ===== Content ===== */` (168~196줄)
  - `/* ===== 알림 아이콘 ===== */` (197~222줄)
  - `/* ===== 유저 드롭다운 ===== */` (223~341줄)
  - `/* ===== Mobile Menu Button ===== */` (1182~1215줄)

**라인 번호는 원본 `style.css` 기준입니다.** `00_legacy.css`에서 찾을 때는 해당 주석 헤더(`/* ===== Sidebar ===== */` 등)로 grep하세요.

- [ ] **Step 2: `01_tokens.css` populate**

Edit `api/static/css/src/01_tokens.css` — placeholder 주석을 지우고 `:root { ... }` 블록을 복사해 넣는다. 원본에서 `:root {` 부터 닫는 `}`까지 그대로.

Example (actual content depends on current legacy):
```css
:root {
    --bg: #0f1117;
    --bg-card: #1a1d27;
    --bg-card-hover: #222632;
    --border: #2a2e3a;
    --text: #e4e6eb;
    --text-muted: #9ba0b4;
    --accent: #4f8cff;
    --accent-hover: #6ba0ff;
    --green: #34d399;
    --red: #f87171;
    --orange: #fbbf24;
    --purple: #9382ff;
    --sidebar-w: 240px;
    /* 의미 기반 alias (누락된 --success/--danger/--warning 보강) */
    --success: #34d399;
    --danger: #f87171;
    --warning: #fbbf24;
    --color-positive: var(--green);
    --color-negative: var(--red);
    /* ... 나머지 원본 그대로 ... */
}
```

- [ ] **Step 3: `02_base.css` populate**

Edit `api/static/css/src/02_base.css` — `/* ===== Reset & Base ===== */` 섹션에서 `:root` 블록을 제외한 나머지(예: `* { box-sizing: border-box; }`, `body`, `a`, scrollbar 등)를 이동.

- [ ] **Step 4: `03_layout.css` populate**

Edit `api/static/css/src/03_layout.css` — 위 Step 1에서 식별한 6개 섹션을 원본 순서대로 이동. 각 섹션 사이에 원본 주석 헤더(`/* ===== Sidebar ===== */` 등) 유지.

- [ ] **Step 5: `00_legacy.css`에서 이동한 섹션 제거**

Edit `api/static/css/src/00_legacy.css` — Step 1에서 식별한 모든 섹션(`:root`, Reset & Base, Sidebar, 그룹 타이틀, Content, 알림 아이콘, 유저 드롭다운, Mobile Menu Button)을 삭제.

**주의:** Mobile Menu Button은 원래 중간(1182줄 부근)에 있는 섹션이므로 legacy 파일의 위치를 정확히 찾아 삭제. 다른 섹션은 legacy 앞부분에 모여있음.

- [ ] **Step 6: 재빌드**

Run:
```bash
venv/Scripts/python -m tools.build_css
```

Expected: `[css] bundled 20 files → ...` — 파일 수는 아직 20개(00_legacy + 01~19).

- [ ] **Step 7: CSS rule parity 검증**

Run:
```bash
venv/Scripts/python -m tools.c1_css_split.capture_css > tools/c1_css_split/after_t3.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('tools/c1_css_split/after_t3.json'))
print(f'baseline: {base[\"total_rules\"]}, after: {after[\"total_rules\"]}')
assert base['total_rules'] == after['total_rules']
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    if b['selector'] != a['selector'] or b['decls_hash'] != a['decls_hash']:
        print(f'MISMATCH at position {i}:')
        print(f'  base:  {b[\"selector\"][:80]!r} hash={b[\"decls_hash\"]}')
        print(f'  after: {a[\"selector\"][:80]!r} hash={a[\"decls_hash\"]}')
        raise SystemExit(1)
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm tools/c1_css_split/after_t3.json
```

Expected: `parity OK — <N> rules identical`

If MISMATCH: the selector order or content changed during move. Investigate with git diff on `00_legacy.css` + `01/02/03_*.css` — likely a selector was skipped or duplicated.

- [ ] **Step 8: API 서버 import 스모크**

Run:
```bash
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `api.main ok`

- [ ] **Step 9: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 01_tokens + 02_base + 03_layout 이동

:root 변수 → 01_tokens.css
Reset & Base → 02_base.css
Sidebar + 그룹 타이틀 + Content + 알림 아이콘 + 유저 드롭다운 + Mobile Menu Button → 03_layout.css

00_legacy.css에서 해당 섹션 제거. rule parity 검증 통과.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `04_cards_grids` + `05_badges` + `06_tables_bars` 이동

**Files:**
- Modify: `api/static/css/src/04_cards_grids.css` (populate)
- Modify: `api/static/css/src/05_badges.css` (populate)
- Modify: `api/static/css/src/06_tables_bars.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove moved sections)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상 식별**

Sections to move:

**→ `04_cards_grids.css`:**
- `/* ===== Cards ===== */` (원본 342~363)
- `/* ===== Stat Grid (Dashboard) ===== */` (364~391)
- `/* ===== Market Summary ===== */` (392~411)
- `/* ===== Summary Body (구조화된 시장 요약) ===== */` (412~497)
- `/* ===== Stat Grid 확장 (6열) ===== */` (498~511)
- `/* ===== Summary Insights (발굴유형 + 섹터) ===== */` (512~602)

**→ `05_badges.css`:**
- `/* ===== Badges ===== */` (603~627)
- `/* ===== Vendor Tier Badges ===== */` (628~632)
- `/* ===== Theme Type / Validity Badges ===== */` (633~638)
- `/* ===== Importance Stars ===== */` (639~641)
- `/* ----- 티어 배지 (Plan badge) ----- */` (1934~1970)
- `/* ----- 등급 배지 (S/A/B/C/D) — UI-14 ----- */` (2406~2457)
- `/* ----- Role / Status Badges ----- */` (2657~2663)

**→ `06_tables_bars.css`:**
- `/* ===== Tables ===== */` (642~667)
- `/* ── Screener Table (Proposals, Sessions) ── */` (668~676)
- `/* ===== Confidence Bar ===== */` (677~695)
- `/* ===== Allocation Bar ===== */` (696~714)
- `/* ===== Indicators ===== */` (715~731)
- `/* ── 변화량 인디케이터 (통일) ── */` (3101~3122)
- `/* ── Sparkline (stat-card 내부) ── */` (3123~3129)

- [ ] **Step 2: 각 섹션을 `00_legacy.css`에서 해당 타겟 파일로 이동**

For each section:
1. Read `00_legacy.css`, locate the section by its comment header (e.g. `/* ===== Cards ===== */`)
2. Copy the section (including its comment header) to the target file, appending to the end
3. Delete the section from `00_legacy.css`

**CRITICAL**: Copy verbatim — no SQL, wait, no CSS changes, no reformatting, no "improvements". Byte-for-byte move.

Order within each target file: maintain original order as it appeared in legacy (by line number ascending). This preserves cascade.

- [ ] **Step 3: 재빌드**

Run:
```bash
venv/Scripts/python -m tools.build_css
```

Expected: `[css] bundled 20 files → ...`

- [ ] **Step 4: Parity 검증**

Run:
```bash
venv/Scripts/python -m tools.c1_css_split.capture_css > tools/c1_css_split/after_t4.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('tools/c1_css_split/after_t4.json'))
print(f'baseline: {base[\"total_rules\"]}, after: {after[\"total_rules\"]}')
assert base['total_rules'] == after['total_rules']
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    if b['selector'] != a['selector'] or b['decls_hash'] != a['decls_hash']:
        print(f'MISMATCH at position {i}:')
        print(f'  base:  {b[\"selector\"][:80]!r} hash={b[\"decls_hash\"]}')
        print(f'  after: {a[\"selector\"][:80]!r} hash={a[\"decls_hash\"]}')
        raise SystemExit(1)
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm tools/c1_css_split/after_t4.json
```

Expected: `parity OK — <N> rules identical`

- [ ] **Step 5: API 스모크**

Run:
```bash
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `api.main ok`

- [ ] **Step 6: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 04_cards_grids + 05_badges + 06_tables_bars 이동

Cards, Stat Grid(6열 포함), Market Summary → 04_cards_grids.css
Badges 일체 (기본/Tier/Theme/Importance/Grade/Role/Status) → 05_badges.css
Tables, Bars, Indicators, Sparkline → 06_tables_bars.css

00_legacy.css에서 제거. rule parity 통과.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: `07_modals` + `08_buttons_forms` 이동

**Files:**
- Modify: `api/static/css/src/07_modals.css` (populate)
- Modify: `api/static/css/src/08_buttons_forms.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상**

**→ `07_modals.css`:**
- `/* ----- 업그레이드 모달 ----- */` (1971~2048)
- `/* ----- 범용 모달 (compact) ----- */` (2049~2083)
- `/* ----- Toast / Result Messages ----- */` (2714~2735)
- `/* ----- Confirm Modal (공용) ----- */` (2736~2780)

**→ `08_buttons_forms.css`:**
- `/* ===== Filters ===== */` (829~886)
- `/* ===== Empty State ===== */` (902~911)
- `/* ----- Button Variants ----- */` (2664~2684)
- `/* ----- Pagination ----- */` (2685~2713)
- `/* ── 외부 사이트 링크 ── */` (2872~2922)

- [ ] **Step 2: 섹션 이동**

Copy verbatim from `00_legacy.css` to target files, then delete from legacy. Maintain original ordering within each target file.

- [ ] **Step 3: 재빌드 + Parity + 스모크**

Run:
```bash
venv/Scripts/python -m tools.build_css
venv/Scripts/python -m tools.c1_css_split.capture_css > ./t5.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('./t5.json'))
assert base['total_rules'] == after['total_rules'], f'{base[\"total_rules\"]} != {after[\"total_rules\"]}'
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    assert b['selector'] == a['selector'] and b['decls_hash'] == a['decls_hash'], f'mismatch @{i}'
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm ./t5.json
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `parity OK` + `api.main ok`

- [ ] **Step 4: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 07_modals + 08_buttons_forms 이동

업그레이드/범용/확인 모달 + Toast → 07_modals.css
Filters + Button Variants + Pagination + Empty State + 외부 링크
  → 08_buttons_forms.css

00_legacy.css에서 제거. rule parity 통과.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `09_proposals` + `10_themes` 이동

**Files:**
- Modify: `api/static/css/src/09_proposals.css` (populate)
- Modify: `api/static/css/src/10_themes.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상**

**→ `09_proposals.css`:**
- `/* ===== Detail Sections ===== */` (732~740)
- `/* ===== Proposal Card ===== */` (741~828)
- `/* ===== Proposal Row ===== */` (887~901)
- `/* ===== Price Target / Score Display ===== */` (1018~1068)
- `/* ===== Tracking Badges ===== */` (1109~1146)
- `/* ===== 제안 상세 탭 분할 (UI-12) + Sticky CTA (UI-13) ===== */` (2189~2315)

**→ `10_themes.css`:**
- `/* ===== Scenario Cards ===== */` (933~974)
- `/* ===== Macro Impact Table ===== */` (975~979)
- `/* ===== Issue Impact Timeline ===== */` (980~1017)
- `/* ── 테마 신뢰도 프로그레스 링 ── */` (3618~3623)
- `/* ── 발굴 유형 도넛 ── */` (3624~3655)
- `/* ── 섹터 버블 ── */` (3656~3688)

- [ ] **Step 2: 이동**

Verbatim copy with original order preservation.

- [ ] **Step 3: 재빌드 + Parity + 스모크**

```bash
venv/Scripts/python -m tools.build_css
venv/Scripts/python -m tools.c1_css_split.capture_css > ./t6.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('./t6.json'))
assert base['total_rules'] == after['total_rules']
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    assert b['selector'] == a['selector'] and b['decls_hash'] == a['decls_hash'], f'mismatch @{i}: {b[\"selector\"][:60]!r}'
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm ./t6.json
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `parity OK` + `api.main ok`

- [ ] **Step 4: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 09_proposals + 10_themes 이동

Detail Sections, Proposal Card/Row, Price Target, Tracking Badges,
제안 상세 탭+Sticky CTA → 09_proposals.css

Scenario Cards, Macro Impact, Issue Timeline, 테마 신뢰도 링,
발굴 유형 도넛, 섹터 버블 → 10_themes.css

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: `11_dashboard_hero` + `12_dashboard_rest` 이동

**Files:**
- Modify: `api/static/css/src/11_dashboard_hero.css` (populate)
- Modify: `api/static/css/src/12_dashboard_rest.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상**

**→ `11_dashboard_hero.css`:**
- `/* ── Tier 1 Hero 레이아웃 ── */` (3093~3100)
- `/* Row 1: KPI Strip — stat-grid-6의 컴팩트 변형 */` (3220~3240)
- `/* Row 1 — KPI + Track Record 통합 (2-col) */` (3241~3250)
- `/* KPI Strip Tight ... */` (3251~3279)
- `/* Row 2/3 공통: 2-column grid */` (3280~3295)
- `/* Row 2 (3-col 단일 행) */` (3296~3307)
- `/* 중간 너비 */` (3308~3309)
- `/* Hero 내부 Track Record 위젯 ... */` (3310~3315)
- `/* ── 발굴 유형 분포 스택바 (Hero 압축용) ── */` (3316~3368)
- `/* ── 섹터 칩 (Top 5) ── */` (3369~3404)
- `/* ── Insight Card Compact (Hero Row 3) ── */` (3405~3412)
- `/* ── Yield Curve 차트 ── */` (3689~3691)
- `/* ── Market Summary 접이식 ── */` (3692~3716)

**→ `12_dashboard_rest.css`:**
- `/* ===== Signals (Dashboard) ===== */` (1069~1108)
- `/* ===== News Section (Dashboard) ===== */` (1569~1662)
- `/* ===== 워치리스트 토글 ===== */` (1743~1753)
- `/* ===== Top Picks (대시보드 오늘의 추천) ===== */` (1754~1928)
- `/* ----- Dashboard Track Record 위젯 — UI-11 ----- */` (2316~2372)
- `/* ----- 카드 잠금 오버레이 (Free Blur) — UI-11 ----- */` (2373~2405)
- `/* ── 워치리스트 요약 위젯 ── */` (3144~3215)
- `/* ── Track Record 시각화 (도넛 + 바) ── */` (3413~3469)
- `/* ── Top Picks 불릿 차트 ── */` (3470~3524)
- `/* ── Top Picks 3-Zone 카드 강화 ── */` (3525~3617)

- [ ] **Step 2: 이동**

Verbatim copy.

- [ ] **Step 3: 재빌드 + Parity + 스모크**

```bash
venv/Scripts/python -m tools.build_css
venv/Scripts/python -m tools.c1_css_split.capture_css > ./t7.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('./t7.json'))
assert base['total_rules'] == after['total_rules']
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    assert b['selector'] == a['selector'] and b['decls_hash'] == a['decls_hash'], f'mismatch @{i}'
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm ./t7.json
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `parity OK` + `api.main ok`

- [ ] **Step 4: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 11_dashboard_hero + 12_dashboard_rest 이동

Tier1 Hero, KPI Strip, Row 1-3, Hero 위젯, 발굴 유형 분포, 섹터 칩,
Insight Card Compact, Yield Curve, Market Summary 접이식
  → 11_dashboard_hero.css

Signals, News Section, 워치리스트 토글, Top Picks 카드/불릿/3-Zone,
Track Record 위젯 + 도넛/바, 카드 잠금, 워치리스트 요약 위젯
  → 12_dashboard_rest.css

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `13_stock_analysis` + `14_chat` 이동

**Files:**
- Modify: `api/static/css/src/13_stock_analysis.css` (populate)
- Modify: `api/static/css/src/14_chat.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상**

**→ `13_stock_analysis.css`:**
- `/* ═══... Stock Analysis ... ═══ */` 헤더 (3774~3781 근처)
- `/* ── Hero: 상단 요약 ... ── */` (3782~3881)
- `/* ── Meta chips bar ── */` (3882~3907)
- `/* ── TOC (섹션 퀵 네비) ── */` (3908~3945)
- `/* ── Section cards ── */` (3946~3955)
- `/* ── 재무/팩터 테이블 ── */` (3956~3962)
- `/* ── Bull / Bear ── */` (3963~3974)
- `/* ── Risk card ── */` (3975~3981)
- `/* ── 진입/청산 조건 ── */` (3982~4006)
- `/* ── Full report pre ── */` (4007~4023)
- `/* ── Full report (markdown → HTML) ── */` (4024~4111)
- `/* Markdown 표 */` (4112~4151)

(주의: `/* ── 모바일 반응형 ── */` (4152~끝)은 Stock Analysis 전용 모바일 규칙 — 19_responsive.css로 이동하는 것이 원칙이나, 페이지 전용이므로 13_stock_analysis.css에 함께 두는 것도 허용됨. **이번 플랜에서는 19_responsive.css로 보내는 것을 기준으로 한다** — Task 10 참조.)

**→ `14_chat.css`:**
- `/* ===== Theme Chat ===== */` (1357~1504)
- `/* 채팅 목록 카드 */` (1505~1568)

- [ ] **Step 2: 이동**

Verbatim copy.

- [ ] **Step 3: 재빌드 + Parity + 스모크**

```bash
venv/Scripts/python -m tools.build_css
venv/Scripts/python -m tools.c1_css_split.capture_css > ./t8.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('./t8.json'))
assert base['total_rules'] == after['total_rules']
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    assert b['selector'] == a['selector'] and b['decls_hash'] == a['decls_hash'], f'mismatch @{i}'
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm ./t8.json
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `parity OK` + `api.main ok`

- [ ] **Step 4: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 13_stock_analysis + 14_chat 이동

Stock Analysis 전용: Hero, Meta chips, TOC, Section cards,
재무/팩터 테이블, Bull/Bear, Risk card, 진입/청산 조건,
Full report (pre + markdown → HTML + Markdown 표)
  → 13_stock_analysis.css

Theme Chat + 채팅 목록 카드 → 14_chat.css

Stock Analysis 모바일 반응형은 T10에서 19_responsive.css로 이동.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: `15_admin` + `16_inquiry` + `17_history` 이동

**Files:**
- Modify: `api/static/css/src/15_admin.css` (populate)
- Modify: `api/static/css/src/16_inquiry.css` (populate)
- Modify: `api/static/css/src/17_history.css` (populate)
- Modify: `api/static/css/src/00_legacy.css` (remove)
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상**

**→ `15_admin.css`:**
- `/* ===== Session Card Top ===== */` (1216~1243)
- `/* ===== Admin Page ===== */` (1663~1742)
- `/* ----- User Admin: 확장 패널 ----- */` (2781~2839)
- `/* ----- User Admin: 모바일 반응형 ----- */` (2840~2871)

**→ `16_inquiry.css`:**
- `/* ── 고객 문의 게시판 ────────────────────────── */` (2923~2967)
- `/* 문의 상태/카테고리 배지 */` (2968~2977)
- `/* 페이지네이션 */` (2978~2990)
- `/* 문의 작성 폼 */` (2991~3020)
- `/* 문의 상세 */` (3021~3041)
- `/* 답변/코멘트 */` (3042~3071)
- `/* 모바일 대응 */` (3072~3088)

**→ `17_history.css`:**
- `/* ===== History Timeline ===== */` (1147~1181)

- [ ] **Step 2: 이동**

Verbatim copy.

- [ ] **Step 3: 재빌드 + Parity + 스모크**

```bash
venv/Scripts/python -m tools.build_css
venv/Scripts/python -m tools.c1_css_split.capture_css > ./t9.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('./t9.json'))
assert base['total_rules'] == after['total_rules']
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    assert b['selector'] == a['selector'] and b['decls_hash'] == a['decls_hash'], f'mismatch @{i}'
print(f'parity OK — {base[\"total_rules\"]} rules identical')
"
rm ./t9.json
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `parity OK` + `api.main ok`

- [ ] **Step 4: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 15_admin + 16_inquiry + 17_history 이동

Session Card Top, Admin Page, User Admin(확장+모바일)
  → 15_admin.css

고객 문의 게시판 전체 (목록/배지/페이지네이션/작성/상세/답변/모바일)
  → 16_inquiry.css

History Timeline → 17_history.css

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: `18_mobile_features` + `19_responsive` 이동 + `00_legacy.css` 삭제

마지막 이동 + legacy 파일 제거.

**Files:**
- Modify: `api/static/css/src/18_mobile_features.css` (populate)
- Modify: `api/static/css/src/19_responsive.css` (populate)
- Delete: `api/static/css/src/00_legacy.css`
- Modify: `api/static/css/style.css` (rebuild)

- [ ] **Step 1: 이동 대상**

**→ `18_mobile_features.css`:**
- `/* ===== Risk Temperature ===== */` (912~932)
- `/* ----- 면책 배너 (footer) ----- */` (2084~2119)
- `/* ----- 하단 탭바 (모바일 전용) ----- */` (2120~2171)
- `/* ----- 터치 타겟 44px (UI-07) — 컨텍스트 한정 ----- */` (2172~2180)
- `/* ----- 세이프 에어리어 (iOS notch 대응, UI-09) ----- */` (2181~2188)
- `/* ----- Free 광고 슬롯 — UI-17 ----- */` (2458~2513)
- `/* ----- 모바일 테이블 → 라벨 스택 변환 (UI-15) ----- */` (2536~2576)
- `/* ── 리스크 온도 SVG 반원형 게이지 ── */` (3130~3143)

**→ `19_responsive.css`:**
- `/* ===== Responsive ===== */` (1244~1356) — 기본 responsive 블록
- `/* ----- Tablet 브레이크포인트 (769~1024px) — UI-18 ----- */` (2514~2535)
- `/* ----- 모바일 전용 추가 규칙 (하단 탭바 노출 + 여백 확보) ----- */` (2577~2652)
- `/* ── 모바일 반응형 ── */` (Tier1 Hero 쪽, 3717~3773)
- `/* ── 모바일 반응형 ── */` (Stock Analysis 쪽, 4152~끝)

- [ ] **Step 2: 이동 — 00_legacy.css가 완전히 비어야 함**

이동 완료 후 `00_legacy.css`에 남은 내용 확인:
```bash
wc -l api/static/css/src/00_legacy.css
grep -v "^\s*/\*\|^\s*$\|^\s*\*" api/static/css/src/00_legacy.css | head -20
```

Expected: 남은 실제 CSS 규칙이 **0건**. 주석이나 빈 줄만 있다면 OK. 만약 뭐가 남아있다면, 어느 파일로 가야 할지 판단해서 이동 후 반복.

- [ ] **Step 3: `00_legacy.css` 삭제**

Run:
```bash
git rm api/static/css/src/00_legacy.css
```

- [ ] **Step 4: 재빌드**

```bash
venv/Scripts/python -m tools.build_css
```

Expected: `[css] bundled 19 files → ...` — 이제 legacy 없이 19개.

- [ ] **Step 5: Final CSS rule parity — 가장 중요한 검증**

Run:
```bash
venv/Scripts/python -m tools.c1_css_split.capture_css > ./t10_final.json
venv/Scripts/python -c "
import json
base = json.load(open('tools/c1_css_split/baseline.json'))
after = json.load(open('./t10_final.json'))
print(f'baseline: {base[\"total_rules\"]} rules')
print(f'after:    {after[\"total_rules\"]} rules')
assert base['total_rules'] == after['total_rules'], 'rule count mismatch'
for i, (b, a) in enumerate(zip(base['rules'], after['rules'])):
    if b['selector'] != a['selector'] or b['decls_hash'] != a['decls_hash']:
        print(f'MISMATCH at position {i}:')
        print(f'  base:  {b[\"selector\"][:80]!r} hash={b[\"decls_hash\"]}')
        print(f'  after: {a[\"selector\"][:80]!r} hash={a[\"decls_hash\"]}')
        raise SystemExit(1)
print(f'FINAL parity OK — {base[\"total_rules\"]} rules identical')
"
rm ./t10_final.json
```

Expected: `FINAL parity OK — <N> rules identical`

- [ ] **Step 6: `!important` 개수 검증**

Run:
```bash
grep -c "!important" api/static/css/style.css
```

Expected: T1 Step 5에서 기록한 baseline 개수와 **동일**.

- [ ] **Step 7: 빌드 결정성 검증**

Run:
```bash
venv/Scripts/python -m tools.build_css
sha1=$(sha256sum api/static/css/style.css | awk '{print $1}')
venv/Scripts/python -m tools.build_css
sha2=$(sha256sum api/static/css/style.css | awk '{print $1}')
echo "sha1=$sha1"
echo "sha2=$sha2"
[ "$sha1" = "$sha2" ] && echo "deterministic OK" || echo "FAIL: sha differ"
```

Expected: `deterministic OK`

On Windows bash without `sha256sum`, use:
```bash
venv/Scripts/python -c "
import hashlib, subprocess
from pathlib import Path
subprocess.check_call(['venv/Scripts/python', '-m', 'tools.build_css'])
h1 = hashlib.sha256(Path('api/static/css/style.css').read_bytes()).hexdigest()
subprocess.check_call(['venv/Scripts/python', '-m', 'tools.build_css'])
h2 = hashlib.sha256(Path('api/static/css/style.css').read_bytes()).hexdigest()
assert h1 == h2, f'non-deterministic: {h1} vs {h2}'
print(f'deterministic OK — {h1}')
"
```

- [ ] **Step 8: 최종 API 스모크**

```bash
venv/Scripts/python -c "import api.main; print('api.main ok')"
```

Expected: `api.main ok`

- [ ] **Step 9: 각 src 파일 라인수 확인**

```bash
wc -l api/static/css/src/*.css
```

Expected: 19개 파일, 최대 500줄 이내 (스펙 목표). 만약 크게 넘는 파일이 있으면 내역 기록.

- [ ] **Step 10: Commit**

```bash
git add api/static/css/src/ api/static/css/style.css
git commit -m "$(cat <<'EOF'
refactor(css): C1 — 18_mobile_features + 19_responsive + legacy 삭제

Risk Temperature, 면책 배너, 하단 탭바, Touch target, Safe area,
Free 광고 슬롯, Mobile table stack, Risk Temperature SVG
  → 18_mobile_features.css

Responsive base + Tablet(UI-18) + 모바일 전용 규칙 +
Dashboard Hero 모바일 + Stock Analysis 모바일
  → 19_responsive.css

00_legacy.css 삭제 — CSS 분할 완료. FINAL parity 검증 통과,
빌드 결정성 확인, !important 개수 baseline과 동일.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: 검증 완료 메모 + 브라우저 시각 점검 체크리스트

**Files:**
- Create: `_docs/20260420_C1_css_split_검증완료.md`

- [ ] **Step 1: 메트릭 수집**

```bash
wc -l api/static/css/src/*.css
grep -c "!important" api/static/css/style.css
venv/Scripts/python -m tools.c1_css_split.capture_css | venv/Scripts/python -c "
import json, sys
d = json.load(sys.stdin)
print(f'final rule count: {d[\"total_rules\"]}')
"
```

- [ ] **Step 2: 메모 작성**

Create `_docs/20260420_C1_css_split_검증완료.md`:

```markdown
# C1 — style.css 분할 검증 완료 메모

- **일자**: 2026-04-20
- **스펙**: `docs/superpowers/specs/2026-04-20-c1-css-split-design.md`
- **플랜**: `docs/superpowers/plans/2026-04-20-c1-css-split.md`

## 결과 요약

| 항목 | 분할 전 | 분할 후 |
|---|---|---|
| 파일 수 | 1 (`style.css` 4,165줄) | 19 (`src/*.css`) + 빌드 결과물 1 |
| 최대 파일 라인 | 4,165 | [실측 — 위 wc -l 결과 중 최대값] |
| CSS 규칙 수 | [baseline 값] | [final 값 — 동일해야 함] |
| `!important` 개수 | [baseline] | [동일] |
| HTTP 요청 수 | 1 (style.css) | 1 (번들된 style.css) |
| 빌드 결정성 | — | ✅ (2회 연속 실행 시 SHA-256 동일) |

## 검증 통과 항목

- [x] CSS rule parity — 선택자 + 속성 해시 + 순서 완전 일치 (`tools/c1_css_split/capture_css.py` 기반)
- [x] `!important` 개수 동일
- [x] 빌드 결정성 — `python -m tools.build_css` 연속 실행 시 SHA-256 동일
- [x] `api.main` import 성공
- [x] `api/static/css/src/` 19개 파일, 최대 500줄 이내 [실측에 따라 체크]
- [x] `api/static/css/style.css` linguist-generated 표시
- [x] `api/static/css/README.md` 가이드 작성

## 브라우저 시각 회귀 점검 (수동)

데스크톱(1440px), 태블릿(900px), 모바일(375px) 3종 viewport에서 다음 페이지 점검:

- [ ] `/` — 대시보드 (Hero + KPI + Track Record + Top Picks + News)
- [ ] `/sessions` — 세션 목록
- [ ] `/themes` — 테마 목록 + 필터
- [ ] `/proposals` — 제안 목록 + 상세 탭 + Sticky CTA
- [ ] `/proposals/<id>/stock-analysis` — 종목 심층분석
- [ ] `/admin` — 관리자
- [ ] `/admin/users` — 유저 관리 (확장 패널 포함)
- [ ] `/chat` — 테마 채팅
- [ ] `/inquiry` — 고객 문의
- [ ] 업그레이드 모달 열림 시 레이아웃
- [ ] 하단 탭바(모바일) 정렬 + 활성 인디케이터
- [ ] 면책 배너(푸터)

각 페이지 점검 결과: [점검 시 기록]

## 작업 커밋

(git log --oneline --grep "C1" 결과를 그대로 붙여넣기)

## 남은 관심사 (후속 트랙 C2·C3)

- **C2**: 템플릿/매크로 분할 (`_macros.html` 655줄, `dashboard.html` 658줄, `admin.html` 486줄 등 → 도메인별 partial/macro 추출)
- **C3**: 인라인 JavaScript 분할 (`base.html`의 fetch 인터셉터, 401 refresh, 업그레이드 모달 열기 등 → `api/static/js/`로 추출)

## 다음 단계

C2(템플릿/매크로 분할) 브레인스토밍 → 구현.
```

- [ ] **Step 3: Commit**

```bash
git add _docs/20260420_C1_css_split_검증완료.md
git commit -m "$(cat <<'EOF'
docs(refactor): C1 검증 완료 메모 — style.css 분할

19개 파일로 분할 완료. CSS rule parity + 빌드 결정성 검증 통과.
다음 단계는 C2(템플릿·매크로 분할).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 완료 확인 체크리스트

- [ ] Task 1: baseline 캡처
- [ ] Task 2: 빌드 스크립트 + src 스캐폴드 + 00_legacy
- [ ] Task 3: 01_tokens + 02_base + 03_layout 이동
- [ ] Task 4: 04_cards_grids + 05_badges + 06_tables_bars 이동
- [ ] Task 5: 07_modals + 08_buttons_forms 이동
- [ ] Task 6: 09_proposals + 10_themes 이동
- [ ] Task 7: 11_dashboard_hero + 12_dashboard_rest 이동
- [ ] Task 8: 13_stock_analysis + 14_chat 이동
- [ ] Task 9: 15_admin + 16_inquiry + 17_history 이동
- [ ] Task 10: 18_mobile_features + 19_responsive + legacy 삭제
- [ ] Task 11: 검증 완료 메모

**후속:** C2(템플릿·매크로 분할) 브레인스토밍 시작.

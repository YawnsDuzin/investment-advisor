# PostgreSQL 접속 정보 — 이 파일을 .env로 복사한 뒤 값을 수정하세요
# cp .env.example .env  (Linux/Mac)
# copy .env.example .env  (Windows)

DB_HOST=localhost
DB_PORT=5432
DB_NAME=investment_advisor
DB_USER=postgres
DB_PASSWORD=your_password_here

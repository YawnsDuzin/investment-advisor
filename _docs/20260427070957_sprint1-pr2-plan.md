# Sprint 1 — PR-2 Global News Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement task-by-task.

**Goal:** RSS 피드를 region 단위(KR/US/JP/CN/EU/GLOBAL)로 재구조화 + JP/CN/EU 신규 소스 추가 + Stage 1 프롬프트 입력을 region 별로 그룹핑. 번역은 기존 Haiku 파이프라인 재사용.

**Architecture:** `NewsConfig.feeds` dict 를 `feed_sources: list[FeedSpec]` flat list 로 리팩토 — 각 entry 에 `url/lang/region/category` 메타데이터 부착. `news_collector` 가 feed_sources 를 순회하며 article 에 `lang/region/title_original` 태그 + region 별 그룹 text 출력. `news_repo.save_news_articles` 가 v41 신규 3 컬럼(`lang/region/title_original`) 을 persist. 번역 로직은 무수정 — 기존 `_translate_news_batch()` 가 한글 외 언어 자동 감지 + Haiku 번역하는 구조 그대로.

**Tech Stack:** Python 3.10+ / dataclasses / feedparser (mocked) / pytest / Claude Haiku via existing SDK pipeline

**관련 spec:** [_docs/20260427055258_sprint1-design.md](20260427055258_sprint1-design.md) §6 (Feature J)

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `shared/config.py` | `FeedSpec` dataclass + `NewsConfig.feed_sources: list[FeedSpec]` (KR/US/JP/CN/EU + 기존 카테고리 region 태깅) | Modify |
| `analyzer/news_collector.py` | feed_sources 순회 + 각 article 에 `lang/region/title_original` 태그 + region 별 그룹 text | Modify |
| `shared/db/news_repo.py` | `save_news_articles` 가 `lang/region/title_original` 컬럼 INSERT | Modify (단순 컬럼 추가) |
| `analyzer/prompts.py` | (선택) STAGE1_PROMPT docstring 에 region 그룹 입력 안내 — 본문 변경 없음 (text 가 이미 그룹된 상태로 들어감) | Modify (주석만) |
| `tests/test_news_config_global.py` | FeedSpec 스키마 + feed_sources 분포 + GLOBAL_NEWS_ENABLED 토글 | **Create** |
| `tests/test_news_collector_global.py` | Article 태깅 + region 그룹 text + GLOBAL_NEWS_ENABLED off 시 KR+US 만 | **Create** |
| `tests/test_news_repo_v41.py` | `save_news_articles` 가 v41 3 컬럼 persist | **Create** |

---

## Tasks

### Task 1: `FeedSpec` + `feed_sources` failing test

**Files:**
- Create: `tests/test_news_config_global.py`

#### Steps

- [ ] **Step 1.1: failing test 작성**

`tests/test_news_config_global.py`:

```python
"""NewsConfig — Sprint 1 PR-2 region-tagged feed sources.

기존 `feeds: dict[str, list[str]]` 를 `feed_sources: list[FeedSpec]` 로 리팩토.
각 FeedSpec 에 url/lang/region/category 메타데이터 부착.
GLOBAL_NEWS_ENABLED env 토글로 KR+US 외 region 활성화 제어.

Spec: _docs/20260427055258_sprint1-design.md §6
"""
import os
from unittest.mock import patch


class TestFeedSpec:
    """FeedSpec dataclass — url/lang/region/category 필드."""

    def test_feedspec_required_fields(self):
        from shared.config import FeedSpec
        spec = FeedSpec(
            url="https://example.com/rss",
            lang="en",
            region="US",
            category="finance",
        )
        assert spec.url == "https://example.com/rss"
        assert spec.lang == "en"
        assert spec.region == "US"
        assert spec.category == "finance"


class TestNewsConfigFeedSources:
    """feed_sources flat list — 모든 region 태그 정확."""

    def test_feed_sources_present(self):
        from shared.config import NewsConfig
        cfg = NewsConfig()
        assert hasattr(cfg, "feed_sources")
        assert isinstance(cfg.feed_sources, list)
        assert len(cfg.feed_sources) > 0

    def test_all_feeds_have_metadata(self):
        from shared.config import NewsConfig, FeedSpec
        cfg = NewsConfig()
        for spec in cfg.feed_sources:
            assert isinstance(spec, FeedSpec)
            assert spec.url
            assert spec.lang in ("ko", "en", "ja", "zh")
            assert spec.region in ("KR", "US", "JP", "CN", "EU", "GLOBAL")
            assert spec.category  # 빈 문자열 아님

    def test_kr_feeds_exist(self):
        from shared.config import NewsConfig
        cfg = NewsConfig()
        kr = [f for f in cfg.feed_sources if f.region == "KR"]
        assert len(kr) >= 2  # 한경 + etnews 등
        for f in kr:
            assert f.lang == "ko"

    def test_us_feeds_exist(self):
        from shared.config import NewsConfig
        cfg = NewsConfig()
        us = [f for f in cfg.feed_sources if f.region == "US"]
        assert len(us) >= 3  # Reuters/Bloomberg/CNBC 등
        for f in us:
            assert f.lang == "en"

    def test_jp_feed_exists(self):
        """Sprint 1 PR-2 — JP 신규 소스."""
        from shared.config import NewsConfig
        cfg = NewsConfig()
        jp = [f for f in cfg.feed_sources if f.region == "JP"]
        assert len(jp) >= 1
        for f in jp:
            assert f.lang in ("en", "ja")

    def test_cn_feed_exists(self):
        """Sprint 1 PR-2 — CN 신규 소스."""
        from shared.config import NewsConfig
        cfg = NewsConfig()
        cn = [f for f in cfg.feed_sources if f.region == "CN"]
        assert len(cn) >= 1

    def test_eu_feed_exists(self):
        """Sprint 1 PR-2 — EU 신규 소스."""
        from shared.config import NewsConfig
        cfg = NewsConfig()
        eu = [f for f in cfg.feed_sources if f.region == "EU"]
        assert len(eu) >= 1

    def test_legacy_feeds_dict_still_available(self):
        """기존 코드 (cfg.feeds[...]) 가 깨지지 않도록 backward-compat property 유지."""
        from shared.config import NewsConfig
        cfg = NewsConfig()
        assert hasattr(cfg, "feeds")
        assert isinstance(cfg.feeds, dict)
        # 카테고리 → URL 리스트 매핑이 feed_sources 와 정합
        for cat, urls in cfg.feeds.items():
            for url in urls:
                # 모든 url 이 feed_sources 어딘가에 존재
                matched = [f for f in cfg.feed_sources if f.url == url]
                assert matched, f"feeds dict 의 {url} 이 feed_sources 에 없음"


class TestGlobalNewsEnabledToggle:
    """GLOBAL_NEWS_ENABLED env 토글."""

    def test_default_includes_global(self):
        """기본값 (GLOBAL_NEWS_ENABLED=true) — JP/CN/EU 모두 포함."""
        from shared.config import NewsConfig
        with patch.dict(os.environ, {}, clear=False):
            os.environ.pop("GLOBAL_NEWS_ENABLED", None)
            cfg = NewsConfig()
            regions = {f.region for f in cfg.active_feed_sources()}
            assert "JP" in regions
            assert "CN" in regions
            assert "EU" in regions

    def test_disabled_excludes_global(self):
        """GLOBAL_NEWS_ENABLED=false — KR/US 만 활성, JP/CN/EU 제외."""
        from shared.config import NewsConfig
        with patch.dict(os.environ, {"GLOBAL_NEWS_ENABLED": "false"}):
            cfg = NewsConfig()
            regions = {f.region for f in cfg.active_feed_sources()}
            assert "KR" in regions
            assert "US" in regions
            assert "JP" not in regions
            assert "CN" not in regions
            assert "EU" not in regions

    def test_active_returns_all_when_enabled(self):
        """GLOBAL_NEWS_ENABLED=true 시 active_feed_sources 가 전체와 동일."""
        from shared.config import NewsConfig
        with patch.dict(os.environ, {"GLOBAL_NEWS_ENABLED": "true"}):
            cfg = NewsConfig()
            assert len(cfg.active_feed_sources()) == len(cfg.feed_sources)
```

- [ ] **Step 1.2: 실행 — 모두 FAIL 확인**

```bash
pytest tests/test_news_config_global.py -v
```

Expected: 11개 테스트 모두 ImportError/AttributeError 로 FAIL.

- [ ] **Step 1.3: Commit (test only)**

```bash
git add tests/test_news_config_global.py
git commit -m "$(cat <<'EOF'
test(sprint1): NewsConfig FeedSpec + region-tagged feed_sources failing test

Sprint 1 PR-2 글로벌 뉴스 — config 레벨 schema 검증.
다음 commit 에서 FeedSpec + feed_sources 구현 시 GREEN.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `FeedSpec` + `feed_sources` 구현

**Files:**
- Modify: `shared/config.py`

#### Steps

- [ ] **Step 2.1: `FeedSpec` 추가**

`shared/config.py` 의 `class NewsConfig:` *바로 위* 에 dataclass 추가:

```python
@dataclass(frozen=True)
class FeedSpec:
    """RSS 피드 메타데이터.

    각 피드는 region/lang/category 로 태깅되어 article persist 시 propagate.
    Sprint 1 PR-2: 단일 source-of-truth 로 NewsConfig.feed_sources 에 flat list 보관.
    """
    url: str
    lang: str        # 'ko' | 'en' | 'ja' | 'zh'
    region: str      # 'KR' | 'US' | 'JP' | 'CN' | 'EU' | 'GLOBAL'
    category: str    # 'finance' | 'technology' | 'commodities' | ... (분류 태그, 자유)
```

- [ ] **Step 2.2: `NewsConfig` 본문 교체**

`shared/config.py` 의 `class NewsConfig:` 본문 (line 43-83) 을 다음으로 교체:

```python
@dataclass
class NewsConfig:
    """RSS 피드 소스 설정 — region-tagged flat list.

    Sprint 1 PR-2: feeds dict 를 feed_sources 로 리팩토.
    backward-compat 으로 feeds dict property 유지.
    GLOBAL_NEWS_ENABLED 토글로 JP/CN/EU 활성화 제어.
    """
    feed_sources: list[FeedSpec] = field(default_factory=lambda: [
        # ── KR (한국, ko) ──────────────────────────────
        FeedSpec("https://www.hankyung.com/feed/economy",      "ko", "KR", "korea"),
        FeedSpec("https://www.hankyung.com/feed/stock",        "ko", "KR", "korea"),
        FeedSpec("https://www.etnews.com/rss/Section901.xml",  "ko", "KR", "korea_early"),
        FeedSpec("https://www.thebell.co.kr/rss/rss_news_all.xml", "ko", "KR", "korea_early"),

        # ── US (미국, en) ──────────────────────────────
        FeedSpec("https://feeds.bbci.co.uk/news/world/rss.xml",        "en", "US", "global"),
        FeedSpec("https://feeds.reuters.com/reuters/worldNews",        "en", "US", "global"),
        FeedSpec("https://feeds.reuters.com/reuters/businessNews",     "en", "US", "finance"),
        FeedSpec("https://feeds.bloomberg.com/markets/news.rss",       "en", "US", "finance"),
        FeedSpec("https://www.cnbc.com/id/10001147/device/rss/rss.html","en", "US", "finance"),
        FeedSpec("https://rss.nytimes.com/services/xml/rss/nyt/Technology.xml", "en", "US", "technology"),
        FeedSpec("https://feeds.arstechnica.com/arstechnica/technology-lab",     "en", "US", "technology"),
        FeedSpec("https://oilprice.com/rss/main",                      "en", "US", "commodities"),
        FeedSpec("https://www.federalregister.gov/documents/search.atom?conditions%5Btype%5D=RULE",
                                                                       "en", "US", "early_signals"),
        FeedSpec("https://www.digitimes.com/rss/daily_news.xml",       "en", "US", "early_signals"),

        # ── JP (일본, en — Nikkei Asia 영문) ────────────
        FeedSpec("https://asia.nikkei.com/rss/feed/nar",               "en", "JP", "asia_business"),

        # ── CN (중국, en — Caixin/Yicai 영문) ───────────
        FeedSpec("https://www.caixinglobal.com/rss/news.xml",          "en", "CN", "china_business"),
        FeedSpec("https://www.yicaiglobal.com/rss/news.xml",           "en", "CN", "china_business"),

        # ── EU (유럽, en — Reuters Europe / FT) ─────────
        FeedSpec("https://www.ft.com/companies?format=rss",            "en", "EU", "eu_companies"),
        FeedSpec("https://feeds.reuters.com/reuters/UKBusinessNews",   "en", "EU", "eu_business"),
    ])
    max_articles_per_feed: int = field(default_factory=lambda: int(os.getenv("MAX_ARTICLES_PER_FEED", "5")))

    @property
    def feeds(self) -> dict[str, list[str]]:
        """backward-compat — 카테고리 → URL 리스트.

        기존 코드 (`cfg.feeds["finance"]`) 가 깨지지 않도록 feed_sources 에서 derive.
        """
        from collections import defaultdict
        out: dict[str, list[str]] = defaultdict(list)
        for spec in self.feed_sources:
            out[spec.category].append(spec.url)
        return dict(out)

    def active_feed_sources(self) -> list[FeedSpec]:
        """GLOBAL_NEWS_ENABLED 토글 적용된 활성 feeds.

        false 시 KR/US 만 반환. 기본값 (true) 에서는 전체 반환.
        """
        if _env_bool("GLOBAL_NEWS_ENABLED", True):
            return list(self.feed_sources)
        return [f for f in self.feed_sources if f.region in ("KR", "US")]
```

(주의: `_env_bool` 은 line 86 에서 정의되어 있어 `NewsConfig` 정의 시점엔 아직 없을 수 있음. `active_feed_sources` 는 메서드 호출 시점에 lookup 하므로 OK — 모듈 로드 후엔 사용 가능.)

- [ ] **Step 2.3: 테스트 재실행 — GREEN 확인**

```bash
pytest tests/test_news_config_global.py -v
```

Expected: 11/11 PASS.

- [ ] **Step 2.4: 회귀 — 기존 `cfg.feeds` 사용 코드 영향 없는지**

```bash
python -c "from shared.config import NewsConfig; c = NewsConfig(); print(list(c.feeds.keys()))"
```

Expected: `['korea', 'korea_early', 'global', 'finance', 'technology', 'commodities', 'early_signals', 'asia_business', 'china_business', 'eu_companies', 'eu_business']` 같은 카테고리 리스트.

- [ ] **Step 2.5: Commit**

```bash
git add shared/config.py
git commit -m "$(cat <<'EOF'
feat(config): NewsConfig — FeedSpec + region-tagged feed_sources

- FeedSpec(url, lang, region, category) frozen dataclass
- feed_sources: KR 4 + US 10 + JP 1 + CN 2 + EU 2 = 19 피드
- active_feed_sources() — GLOBAL_NEWS_ENABLED 토글 (KR/US 만 vs 전체)
- feeds property — 기존 cfg.feeds[cat] 호출 backward-compat

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: `news_collector` failing test (article 태깅 + region 그룹)

**Files:**
- Create: `tests/test_news_collector_global.py`

#### Steps

- [ ] **Step 3.1: failing test 작성**

`tests/test_news_collector_global.py`:

```python
"""news_collector — Sprint 1 PR-2 region 태깅 + 그룹 text.

각 article 에 lang/region/title_original 태그 부착 + region 별 섹션 그룹.
feedparser 는 conftest.py 에서 mock 처리되어 실제 RSS 호출 없음.

Spec: _docs/20260427055258_sprint1-design.md §6
"""
from unittest.mock import MagicMock, patch
import os

import pytest


def _fake_entry(title: str, summary: str, published: str = "Mon, 27 Apr 2026 10:00:00 +0000"):
    e = MagicMock()
    e.get = lambda k, default="": {
        "title": title, "summary": summary, "description": summary,
        "link": "http://x", "published": published,
    }.get(k, default)
    return e


def _fake_feed(source_name: str, entries: list):
    feed = MagicMock()
    feed.feed.get = lambda k, default="": {"title": source_name}.get(k, default)
    feed.entries = entries
    return feed


class TestArticleTagging:
    """수집된 각 article 이 FeedSpec 의 lang/region 을 그대로 받는다."""

    def test_kr_article_tagged_ko_kr(self):
        from shared.config import NewsConfig, FeedSpec
        from analyzer.news_collector import collect_news_structured

        cfg = NewsConfig.__new__(NewsConfig)  # default 우회 — 단일 KR 피드만
        cfg.feed_sources = [FeedSpec("http://kr.example/rss", "ko", "KR", "korea")]
        cfg.max_articles_per_feed = 5

        with patch("analyzer.news_collector.feedparser.parse") as fp:
            fp.return_value = _fake_feed("한국경제",
                [_fake_entry("삼성전자 신제품 출시", "본문 요약")])
            _, articles = collect_news_structured(cfg)

        assert len(articles) == 1
        a = articles[0]
        assert a["lang"] == "ko"
        assert a["region"] == "KR"
        assert a["title_original"] == "삼성전자 신제품 출시"
        assert a["category"] == "korea"

    def test_us_article_tagged_en_us(self):
        from shared.config import NewsConfig, FeedSpec
        from analyzer.news_collector import collect_news_structured

        cfg = NewsConfig.__new__(NewsConfig)
        cfg.feed_sources = [FeedSpec("http://us.example/rss", "en", "US", "finance")]
        cfg.max_articles_per_feed = 5

        with patch("analyzer.news_collector.feedparser.parse") as fp:
            fp.return_value = _fake_feed("Reuters",
                [_fake_entry("Fed signals rate cut", "summary text")])
            _, articles = collect_news_structured(cfg)

        assert articles[0]["lang"] == "en"
        assert articles[0]["region"] == "US"
        assert articles[0]["title_original"] == "Fed signals rate cut"
        assert articles[0]["category"] == "finance"

    def test_jp_article_tagged_jp(self):
        from shared.config import NewsConfig, FeedSpec
        from analyzer.news_collector import collect_news_structured

        cfg = NewsConfig.__new__(NewsConfig)
        cfg.feed_sources = [FeedSpec("http://nikkei.example/rss", "en", "JP", "asia_business")]
        cfg.max_articles_per_feed = 5

        with patch("analyzer.news_collector.feedparser.parse") as fp:
            fp.return_value = _fake_feed("Nikkei Asia",
                [_fake_entry("Toyota beats forecast", "summary")])
            _, articles = collect_news_structured(cfg)

        assert articles[0]["region"] == "JP"


class TestRegionGroupedText:
    """news_text 가 region 별 섹션으로 그룹된다."""

    def _build_cfg(self, *specs):
        from shared.config import NewsConfig
        cfg = NewsConfig.__new__(NewsConfig)
        cfg.feed_sources = list(specs)
        cfg.max_articles_per_feed = 5
        return cfg

    def test_kr_us_grouped_separately(self):
        from shared.config import FeedSpec
        from analyzer.news_collector import collect_news_structured

        cfg = self._build_cfg(
            FeedSpec("http://kr/rss", "ko", "KR", "korea"),
            FeedSpec("http://us/rss", "en", "US", "finance"),
        )
        with patch("analyzer.news_collector.feedparser.parse") as fp:
            def parse_side(url):
                if "kr" in url:
                    return _fake_feed("한경", [_fake_entry("삼성", "내용")])
                return _fake_feed("Reuters", [_fake_entry("Apple", "content")])
            fp.side_effect = parse_side
            news_text, _ = collect_news_structured(cfg)

        # 한국 뉴스 / 미국 뉴스 헤더 둘 다 등장
        assert "한국" in news_text or "[KR" in news_text or "[KOREA" in news_text.upper()
        assert "미국" in news_text or "[US" in news_text.upper()
        # 한국 섹션이 미국 섹션보다 위 (또는 어떤 안정 정렬)
        # — 강한 ordering 강제 안 함, 둘 다 존재만 검증
        assert "삼성" in news_text
        assert "Apple" in news_text

    def test_global_section_for_jp_cn_eu(self):
        from shared.config import FeedSpec
        from analyzer.news_collector import collect_news_structured

        cfg = self._build_cfg(
            FeedSpec("http://jp/rss", "en", "JP", "asia_business"),
            FeedSpec("http://cn/rss", "en", "CN", "china_business"),
            FeedSpec("http://eu/rss", "en", "EU", "eu_companies"),
        )
        with patch("analyzer.news_collector.feedparser.parse") as fp:
            def parse_side(url):
                name = "Nikkei" if "jp" in url else ("Caixin" if "cn" in url else "FT")
                title = f"{name} headline"
                return _fake_feed(name, [_fake_entry(title, "content")])
            fp.side_effect = parse_side
            news_text, articles = collect_news_structured(cfg)

        assert len(articles) == 3
        # JP/CN/EU 셋이 GLOBAL 단일 섹션 또는 region 별 섹션 — 둘 다 허용
        # 핵심: 셋 다 news_text 에 등장
        assert "Nikkei" in news_text
        assert "Caixin" in news_text
        assert "FT" in news_text


class TestGlobalNewsEnabledIntegration:
    """active_feed_sources() 토글이 collector 에 적용된다."""

    def test_disabled_excludes_jp_cn_eu(self, monkeypatch):
        from shared.config import NewsConfig
        from analyzer.news_collector import collect_news_structured

        monkeypatch.setenv("GLOBAL_NEWS_ENABLED", "false")
        cfg = NewsConfig()  # default feed_sources 사용

        with patch("analyzer.news_collector.feedparser.parse") as fp:
            fp.return_value = _fake_feed("src", [])  # 모든 호출에 빈 entry
            _, articles = collect_news_structured(cfg)

        # parse 가 호출된 URL 들을 region 별로 분류
        called_urls = [c.args[0] for c in fp.call_args_list]
        kr_urls = [u for u in called_urls if "hankyung" in u or "etnews" in u or "thebell" in u]
        global_only = [u for u in called_urls if "nikkei" in u.lower() or "caixin" in u or "yicai" in u]
        assert len(kr_urls) > 0
        assert len(global_only) == 0  # JP/CN 미호출
```

- [ ] **Step 3.2: 실행 — 모두 FAIL**

```bash
pytest tests/test_news_collector_global.py -v
```

Expected: KeyError("lang"/"region"/"title_original") 또는 region 그룹 없음으로 FAIL.

- [ ] **Step 3.3: Commit (test only)**

```bash
git add tests/test_news_collector_global.py
git commit -m "$(cat <<'EOF'
test(sprint1): news_collector region 태깅 + 그룹 text failing test

기존 collector 는 cfg.feeds dict 만 알고 region/lang 모름.
다음 commit 에서 feed_sources 기반 리팩토 후 GREEN.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: `news_collector` 구현 (region 태깅 + 그룹 text)

**Files:**
- Modify: `analyzer/news_collector.py`

#### Steps

- [ ] **Step 4.1: collector 본문 교체**

`analyzer/news_collector.py` 전체를 다음으로 교체:

```python
"""RSS 뉴스 수집 모듈 — region-tagged feed_sources 기반 (Sprint 1 PR-2).

각 article 에 lang/region/title_original 태그 부착.
news_text 는 region 별 섹션으로 그룹 — Stage 1 프롬프트가 region 단위로 인식.
"""
import time
import socket
from collections import defaultdict
from datetime import datetime, timedelta, timezone
from email.utils import parsedate_to_datetime
import feedparser
from shared.config import NewsConfig, FeedSpec
from shared.logger import get_logger


# 카테고리 라벨 (article 메타데이터용 — text 그룹은 region 단위)
CATEGORY_LABELS = {
    "global": "글로벌 종합",
    "finance": "경제·금융·시장",
    "technology": "기술·AI·반도체",
    "commodities": "에너지·원자재",
    "korea": "한국 경제",
    "korea_early": "한국 산업·M&A·자본시장",
    "early_signals": "선행 지표·규제·공급망",
    "asia_business": "아시아 비즈니스",
    "china_business": "중국 비즈니스",
    "eu_companies": "유럽 기업",
    "eu_business": "유럽 비즈니스",
}

# region 별 섹션 헤더 (news_text 그룹용)
REGION_LABELS = {
    "KR": "한국 뉴스",
    "US": "미국 뉴스",
    "JP": "일본 뉴스",
    "CN": "중국 뉴스",
    "EU": "유럽 뉴스",
    "GLOBAL": "글로벌 뉴스",
}

# Stage 1 프롬프트 입력에서 region 그룹 출력 순서
REGION_ORDER = ["KR", "US", "JP", "CN", "EU", "GLOBAL"]


def _clean_html(text: str) -> str:
    """HTML 태그 간이 제거"""
    import re
    text = re.sub(r'<[^>]+>', ' ', text)
    return " ".join(text.split())


def _parse_published(published: str) -> datetime | None:
    """RSS published 문자열을 datetime으로 파싱 (실패 시 None)"""
    if not published:
        return None
    try:
        return parsedate_to_datetime(published)
    except Exception:
        pass
    try:
        return datetime(*time.strptime(published[:25], "%Y-%m-%dT%H:%M:%S")[:6],
                        tzinfo=timezone.utc)
    except Exception:
        return None


def collect_news_structured(cfg: NewsConfig) -> tuple[str, list[dict]]:
    """RSS 피드에서 뉴스를 수집하여 (region-grouped 텍스트, article 리스트) 반환.

    각 article:
      {
        "category", "source", "title", "summary", "link", "published",
        "lang", "region", "title_original"  # ← Sprint 1 PR-2 추가
      }

    news_text 는 region 별 섹션 (`### [한국 뉴스] (N건)`) 으로 그룹.
    """
    articles: list[dict] = []
    seen_titles: set[str] = set()
    cutoff = datetime.now(timezone.utc) - timedelta(hours=24)
    total = 0
    skipped_old = 0
    skipped_dup = 0

    log = get_logger("뉴스")
    _orig_timeout = socket.getdefaulttimeout()
    socket.setdefaulttimeout(30)

    # GLOBAL_NEWS_ENABLED 토글 적용
    feed_specs: list[FeedSpec] = (
        cfg.active_feed_sources() if hasattr(cfg, "active_feed_sources")
        else list(cfg.feed_sources)
    )

    # region 별로 article 누적 — text 그룹 빌드 시 사용
    by_region: dict[str, list[dict]] = defaultdict(list)

    for spec in feed_specs:
        try:
            feed = feedparser.parse(spec.url)
            source = feed.feed.get("title", spec.url)

            for entry in feed.entries[: cfg.max_articles_per_feed]:
                title = entry.get("title", "")
                published = entry.get("published", "")

                # 24시간 필터
                pub_dt = _parse_published(published)
                if pub_dt and pub_dt < cutoff:
                    skipped_old += 1
                    continue

                # 교차 중복 제거
                title_key = title[:30].strip().lower()
                if title_key in seen_titles:
                    skipped_dup += 1
                    continue
                seen_titles.add(title_key)

                summary = _clean_html(
                    entry.get("summary", entry.get("description", ""))
                )
                link = entry.get("link", "")

                article = {
                    "category": spec.category,
                    "source": source,
                    "title": title,
                    "title_original": title,            # 원문 보존 (PR-2)
                    "summary": summary[:1000],
                    "link": link,
                    "published": published,
                    "lang": spec.lang,                  # PR-2
                    "region": spec.region,              # PR-2
                    "_pub_dt": pub_dt,                  # 내부 정렬용
                }
                articles.append(article)
                by_region[spec.region].append(article)
                total += 1

        except socket.timeout:
            log.warning(f"{spec.url} 타임아웃 (30초 초과)")
        except Exception as e:
            log.warning(f"{spec.url} 수집 실패: {e}")

    socket.setdefaulttimeout(_orig_timeout)

    # ── region 별 섹션 빌드 ────────────────────────────
    sections: list[str] = []
    for region in REGION_ORDER:
        region_articles = by_region.get(region, [])
        if not region_articles:
            continue
        label = REGION_LABELS.get(region, region)
        lines: list[str] = []
        for a in region_articles:
            short_date = ""
            if a["_pub_dt"]:
                short_date = f" ({a['_pub_dt'].strftime('%m/%d %H:%M')})"
            elif a["published"]:
                short_date = f" ({a['published'][:16]})"
            cat_label = CATEGORY_LABELS.get(a["category"], a["category"])
            lines.append(
                f"  • [{a['source']}][{cat_label}]{short_date} {a['title']}\n"
                f"    {a['summary'][:300]}"
            )
        sections.append(f"### [{label}] ({len(lines)}건)\n\n" + "\n\n".join(lines))

    # 내부 필드 정리
    for a in articles:
        a.pop("_pub_dt", None)

    log.info(f"총 {total}건 수집 완료 (region {len(by_region)}개)")
    if skipped_old or skipped_dup:
        log.info(f"필터링: 24시간 초과 {skipped_old}건, 중복 {skipped_dup}건 제외")

    news_text = "\n\n---\n\n".join(sections)
    return news_text, articles


def collect_news(cfg: NewsConfig) -> str:
    """RSS 피드에서 뉴스를 수집하여 region-grouped 텍스트로 반환 (하위호환)."""
    news_text, _ = collect_news_structured(cfg)
    return news_text
```

- [ ] **Step 4.2: 테스트 GREEN**

```bash
pytest tests/test_news_collector_global.py tests/test_news_config_global.py -v
```

Expected: 모두 PASS.

- [ ] **Step 4.3: 회귀 검증**

```bash
pytest -v 2>&1 | tail -10
```

기존 실패 카운트 (9 fail / 6 error pre-existing) 변화 없는지 확인. 신규 regression 발견 시 BLOCKED 보고.

- [ ] **Step 4.4: Commit**

```bash
git add analyzer/news_collector.py
git commit -m "$(cat <<'EOF'
feat(news): region-tagged feed_sources 기반 collector

- FeedSpec.lang/region/category 를 article 메타데이터로 propagate
- title_original 원문 보존 (Sprint 1 v41 컬럼)
- news_text 를 region 별 섹션 (한국/미국/일본/중국/유럽) 으로 그룹
- active_feed_sources() 적용 — GLOBAL_NEWS_ENABLED=false 시 KR/US 만

테스트: news_config_global + news_collector_global 모두 PASS.
Spec: _docs/20260427055258_sprint1-design.md §6

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: `news_repo.save_news_articles` v41 컬럼 — failing test

**Files:**
- Create: `tests/test_news_repo_v41.py`

#### Steps

- [ ] **Step 5.1: failing test 작성**

`tests/test_news_repo_v41.py`:

```python
"""news_repo.save_news_articles — v41 lang/region/title_original 컬럼 INSERT 검증.

psycopg2 mock 으로 cur.execute 의 SQL + 파라미터를 캡처하여 검증.
Spec: _docs/20260427055258_sprint1-design.md §6 + §3 (v41)
"""
from unittest.mock import MagicMock, patch


def _make_article(**overrides):
    base = {
        "category": "finance",
        "source": "Reuters",
        "title": "Fed signals rate cut",
        "title_ko": "연준 금리 인하 시사",
        "summary": "Fed officials signaled...",
        "summary_ko": "연준 관계자들이...",
        "link": "http://x",
        "published": "Mon, 27 Apr 2026 10:00:00 +0000",
        "lang": "en",
        "region": "US",
        "title_original": "Fed signals rate cut",
    }
    base.update(overrides)
    return base


class TestSaveNewsArticlesWithV41Columns:
    def test_insert_includes_lang_region_title_original(self):
        from shared.config import DatabaseConfig
        from shared.db.news_repo import save_news_articles

        cur = MagicMock()
        conn = MagicMock()
        conn.cursor.return_value.__enter__.return_value = cur

        with patch("shared.db.news_repo.get_connection", return_value=conn):
            save_news_articles(DatabaseConfig(), session_id=1, articles=[_make_article()])

        # 마지막 execute 호출이 articles INSERT
        last_call = cur.execute.call_args
        sql, params = last_call.args[0], last_call.args[1]
        assert "INSERT INTO news_articles" in sql
        assert "lang" in sql
        assert "region" in sql
        assert "title_original" in sql
        # params 에 lang='en', region='US', title_original=원문 포함
        assert "en" in params
        assert "US" in params
        assert "Fed signals rate cut" in params

    def test_missing_v41_fields_default_to_none(self):
        """기존 article (PR-1 이전 형식) 도 깨지지 않게 — 누락 키는 None."""
        from shared.config import DatabaseConfig
        from shared.db.news_repo import save_news_articles

        cur = MagicMock()
        conn = MagicMock()
        conn.cursor.return_value.__enter__.return_value = cur

        legacy = {
            "category": "finance", "source": "Reuters",
            "title": "x", "title_ko": "x",
            "summary": "y", "summary_ko": "y",
            "link": "http://x", "published": "",
            # lang/region/title_original 누락
        }

        with patch("shared.db.news_repo.get_connection", return_value=conn):
            save_news_articles(DatabaseConfig(), session_id=1, articles=[legacy])

        # 호출 자체가 성공해야 함 — 누락 필드는 None 으로
        assert cur.execute.called
        params = cur.execute.call_args.args[1]
        # None 이 들어있는지 (lang/region/title_original 자리)
        assert None in params

    def test_returns_count(self):
        from shared.config import DatabaseConfig
        from shared.db.news_repo import save_news_articles

        cur = MagicMock()
        conn = MagicMock()
        conn.cursor.return_value.__enter__.return_value = cur

        with patch("shared.db.news_repo.get_connection", return_value=conn):
            n = save_news_articles(
                DatabaseConfig(), session_id=1,
                articles=[_make_article(), _make_article(title="another")]
            )
        assert n == 2
```

- [ ] **Step 5.2: 실행 — 첫 두 테스트 FAIL**

```bash
pytest tests/test_news_repo_v41.py -v
```

Expected: `test_insert_includes_lang_region_title_original` 와 `test_missing_v41_fields_default_to_none` FAIL (현재 INSERT SQL 에 신규 3 컬럼 없음). `test_returns_count` 는 이미 PASS 가능.

- [ ] **Step 5.3: Commit (test only)**

```bash
git add tests/test_news_repo_v41.py
git commit -m "$(cat <<'EOF'
test(sprint1): save_news_articles v41 컬럼 INSERT failing test

lang/region/title_original 가 INSERT SQL/params 에 포함되는지 검증.
다음 commit 에서 news_repo 확장 후 GREEN.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: `news_repo.save_news_articles` 확장

**Files:**
- Modify: `shared/db/news_repo.py`

#### Steps

- [ ] **Step 6.1: `save_news_articles` 본문 교체**

`shared/db/news_repo.py` 의 `save_news_articles` 함수 전체를 다음으로 교체:

```python
def save_news_articles(cfg: DatabaseConfig, session_id: int, articles: list[dict]) -> int:
    """수집된 뉴스 기사를 DB에 저장 (Sprint 1 v41 컬럼 포함).

    Args:
        articles: [{"category", "source", "title", "title_ko",
                    "summary", "summary_ko", "link", "published",
                    "lang", "region", "title_original"}]
                  — lang/region/title_original 누락 시 NULL 저장 (legacy 호환).
    Returns:
        저장된 기사 수
    """
    if not articles:
        return 0

    conn = get_connection(cfg)
    try:
        with conn.cursor() as cur:
            for a in articles:
                cur.execute(
                    """INSERT INTO news_articles
                       (session_id, category, source, title, title_ko,
                        summary, summary_ko, link, published,
                        lang, region, title_original)
                       VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)""",
                    (session_id, a.get("category"), a.get("source"),
                     a.get("title"), a.get("title_ko"),
                     a.get("summary"), a.get("summary_ko"),
                     a.get("link"), a.get("published"),
                     a.get("lang"), a.get("region"), a.get("title_original"))
                )
        conn.commit()
        return len(articles)
    finally:
        conn.close()
```

- [ ] **Step 6.2: 테스트 GREEN**

```bash
pytest tests/test_news_repo_v41.py -v
```

Expected: 3/3 PASS.

- [ ] **Step 6.3: 회귀**

```bash
pytest -v 2>&1 | tail -10
```

신규 regression 없는지.

- [ ] **Step 6.4: Commit**

```bash
git add shared/db/news_repo.py
git commit -m "$(cat <<'EOF'
feat(news): save_news_articles — v41 lang/region/title_original 영속화

INSERT 컬럼 9 → 12. legacy article(누락 키) 은 NULL 로 저장 — 하위호환.

테스트: news_repo_v41 3/3 PASS.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 통합 smoke + prompts.py 주석 + 운영기 가이드

**Files:**
- Modify: `analyzer/prompts.py` (주석만)
- Append: `_docs/_prompts/20260427_prompt.md`

#### Steps

- [ ] **Step 7.1: prompts.py 의 STAGE1_PROMPT 위에 region 그룹 안내 주석**

`analyzer/prompts.py` 의 `STAGE1_PROMPT = """...` 정의 *바로 위* 빈 줄에 다음 한 줄 주석 추가 (그 외 본문 변경 없음):

```python
# news_text 는 news_collector 가 region 별 섹션 ([한국 뉴스] / [미국 뉴스] / [일본 뉴스] ...) 으로 그룹하여 전달 (Sprint 1 PR-2).
```

같은 주석을 `STAGE1A_PROMPT`, `STAGE1A1_PROMPT`, `STAGE1A2_PROMPT` 정의 위에도 (동일 문장으로) 한 줄씩 추가. 4곳 총 4줄.

- [ ] **Step 7.2: 통합 smoke — collector → save 시뮬레이션**

```bash
python -c "
from unittest.mock import MagicMock, patch
from shared.config import NewsConfig, FeedSpec, DatabaseConfig
from analyzer.news_collector import collect_news_structured
from shared.db.news_repo import save_news_articles

cfg = NewsConfig.__new__(NewsConfig)
cfg.feed_sources = [
    FeedSpec('http://kr/x', 'ko', 'KR', 'korea'),
    FeedSpec('http://us/x', 'en', 'US', 'finance'),
    FeedSpec('http://jp/x', 'en', 'JP', 'asia_business'),
]
cfg.max_articles_per_feed = 1

import feedparser
def fake_parse(url):
    f = MagicMock()
    f.feed.get = lambda k, d='': {'title': 'Src ' + url[-2:]}.get(k, d)
    e = MagicMock()
    e.get = lambda k, d='': {'title': 'TITLE-' + url[-2:], 'summary': 'sum', 'link': url, 'published': ''}.get(k, d)
    f.entries = [e]
    return f

with patch.object(feedparser, 'parse', fake_parse):
    text, articles = collect_news_structured(cfg)
    assert all(a.get('region') and a.get('lang') and a.get('title_original') for a in articles), 'tagging failed'
    print('articles tagged:', [(a['region'], a['lang']) for a in articles])
    print('news_text region headers:')
    for line in text.split('\n'):
        if line.startswith('###'):
            print(' ', line)
print('SMOKE OK')
"
```

Expected output (대략):
```
articles tagged: [('KR', 'ko'), ('US', 'en'), ('JP', 'en')]
news_text region headers:
  ### [한국 뉴스] (1건)
  ### [미국 뉴스] (1건)
  ### [일본 뉴스] (1건)
SMOKE OK
```

- [ ] **Step 7.3: 운영기 배포 명령 prompt 기록**

`_docs/_prompts/20260427_prompt.md` *맨 끝* 에 append:

```

========================

[Sprint 1 PR-2 배포 — 운영기 명령]

cd /home/dzp/dzp-main/program/investment-advisor
git pull
sudo systemctl restart investment-advisor-api  # API 라우트는 영향 없음 — 안전 재기동

# 다음 분석 실행 시 자동으로 글로벌 뉴스 수집 + region-grouped Stage 1 입력 사용
# 글로벌 뉴스 끄려면 .env 에 GLOBAL_NEWS_ENABLED=false 후 service restart

# 검증 — 신규 region 컬럼이 채워지는지 (분석 1회 실행 후)
sudo -u postgres psql -d investment_advisor -c \
  "SELECT region, lang, COUNT(*) FROM news_articles
   WHERE created_at >= NOW() - INTERVAL '24 hours'
   GROUP BY region, lang ORDER BY 3 DESC;"

========================
```

- [ ] **Step 7.4: 최종 회귀 + 신규 테스트 일괄**

```bash
pytest tests/test_news_config_global.py tests/test_news_collector_global.py tests/test_news_repo_v41.py -v
```

Expected: 모두 PASS (15+ 테스트).

전체 회귀:
```bash
pytest -v 2>&1 | tail -10
```

기존 9 fail / 6 error 변화 없음 확인.

- [ ] **Step 7.5: Commit (마무리)**

```bash
git add analyzer/prompts.py _docs/_prompts/20260427_prompt.md
git commit -m "$(cat <<'EOF'
docs(sprint1): PR-2 prompts 주석 + 운영기 배포 가이드

- prompts.py 의 STAGE1*_PROMPT 위에 region 그룹 input 안내 주석
- _docs/_prompts/20260427_prompt.md 에 PR-2 배포 명령 + 검증 쿼리

PR-2 신규 테스트 모두 통과, regression 없음.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 7.6: 최종 git log 확인**

```bash
git log --oneline -10
```

PR-2 의 7개 commit 이 시간순 적층되었는지 확인.

PR-2 끝.

---

## Self-Review Checklist (작성자 — plan 작성 후 1회)

- [x] **Spec coverage** — design.md §6 의 RSS 추가 / lang·region 태깅 / region 그룹 prompt / 번역 재사용 모두 task 매핑
- [x] **TDD 준수** — Task 1·3·5 가 failing test, 각 다음 task 가 GREEN
- [x] **No placeholders** — 모든 step 에 실제 코드/명령
- [x] **DRY** — collector 가 region 매핑·라벨을 단일 dict (REGION_LABELS / REGION_ORDER) 로 관리
- [x] **Type consistency** — `FeedSpec(url, lang, region, category)` 가 task 1·2·3·4·5·6 에서 동일 시그니처
- [x] **Bite-sized** — 각 step 2-5분
- [x] **Backward compat** — `cfg.feeds` property + `save_news_articles` 누락 키 None — 기존 호출자 보호

# 프리마켓 브리핑 페이지 v2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `api/templates/briefing.html` 을 섹터 단위 통합 카드(US movers + KR picks 한 카드)로 재구성하고, 헤드라인 + 모닝 코멘트를 페이지 최상단 단일 hero 카드로 합치며, US mover pill에 풀네임 툴팁 + 종목 상세 페이지 링크를 추가한다.

**Architecture:** 백엔드/DB 변경 없음. 단일 Jinja2 템플릿 재구성 + CSS 신규 + 신규 테스트 파일(`tests/test_briefing_template.py`)에서 jinja2 environment 직접 렌더로 검증. `<details>`/`<summary>` 네이티브 사용 — JS 의존 0.

**Tech Stack:** Jinja2 (FastAPI Jinja2Templates), CSS (template inline `<style>`), pytest, jinja2 `DictLoader`/`ChoiceLoader` (테스트용 base.html stub).

**Spec:** `_docs/20260426120614_briefing-v2-design.md`

---

## File Structure

| 파일 | 액션 | 책임 |
|---|---|---|
| `api/templates/briefing.html` | **Modify (전면)** | 페이지 레이아웃·매칭 로직·CSS 모두 |
| `tests/test_briefing_template.py` | **Create** | jinja2 렌더 결과 substring assertion |
| `tests/_fixtures/briefing_sample.py` | **Create** | 테스트용 briefing dict fixture (재사용) |

코드 변경 외 모든 것(라우트, DB, 배치)은 **그대로**.

---

## Task 1: 테스트 인프라 — Jinja2 stub env + briefing fixture

**Files:**
- Create: `tests/_fixtures/__init__.py` (빈 파일)
- Create: `tests/_fixtures/briefing_sample.py`
- Create: `tests/test_briefing_template.py`

- [ ] **Step 1: fixture `__init__.py` 생성**

```bash
mkdir -p tests/_fixtures
touch tests/_fixtures/__init__.py
```

- [ ] **Step 2: 샘플 briefing dict fixture 작성**

`tests/_fixtures/briefing_sample.py`:
```python
"""briefing.html 렌더 테스트용 샘플 데이터.

첨부 이미지(2026-04-26 KST)와 동일한 형상 — 6개 섹터 중
3개는 KR picks 있음(반도체, IT 소프트웨어, IT 하드웨어),
3개는 KR 매칭 없음(전력/원전, 금속·소재, 통신/케이블).
"""
from __future__ import annotations


def make_briefing(
    *,
    with_headline: bool = True,
    with_morning: bool = True,
    with_kr_only_extra: bool = False,
) -> dict:
    """기본 샘플. 플래그로 부분 케이스 생성."""
    bd: dict = {
        "us_summary": {
            "headline": "INTC +23.60% 이례적 급등이 ARM·AMD·QCOM 두 자릿수 랠리를 견인",
            "groups": [
                {
                    "sector_norm": "semiconductors",
                    "label": "반도체 (SEMICONDUCTORS)",
                    "top_movers": [
                        {"ticker": "INTC", "change_pct": 23.60},
                        {"ticker": "ARM",  "change_pct": 14.76},
                        {"ticker": "AMD",  "change_pct": 13.91},
                    ],
                    "catalyst": "INTC 단일 종목 +23.60% 급등이 섹터 전반 동조 매수를 촉발.",
                },
                {
                    "sector_norm": "communication_cable",
                    "label": "통신·케이블 — 실적 쇼크 (COMMUNICATION)",
                    "top_movers": [
                        {"ticker": "CHTR",  "change_pct": -25.50},
                        {"ticker": "CMCSA", "change_pct": -12.90},
                    ],
                    "catalyst": "케이블TV 가입자 이탈 가속 + 분기 대규모 미스로 급락.",
                },
            ],
        },
        "kr_impact": [
            {
                "sector_norm": "semiconductors",
                "label": "반도체 장비·메모리 — 갭 상승 강력",
                "strength": "gap_up_strong",
                "korean_picks": [
                    {
                        "ticker": "000660", "asset_name": "SK하이닉스", "market": "KOSPI",
                        "rationale": "HBM3E·HBM4 공급 과점 지위로 AMD·QCOM AI 가속기 수요 직접 수혜.",
                        "expected_open_change_pct": "+2~4%",
                    },
                    {
                        "ticker": "042700", "asset_name": "한미반도체", "market": "KOSPI",
                        "rationale": "HBM TC-본딩 장비 독점 공급 지위.",
                        "expected_open_change_pct": "+3~5%",
                    },
                ],
                "catalysts_kr": "오전 외국인 순매수 진입 가능성.",
                "related_etfs": ["KODEX 반도체"],
            },
        ],
        "morning_brief": (
            "오늘 챙겨야 할 핵심은 반도체 메모리 흐름. AI 칩 설계~생산 전 레이어 동반 강세, "
            "케이블TV 실적 쇼크는 극명한 대조."
        ),
    }
    if not with_headline:
        bd["us_summary"]["headline"] = ""
    if not with_morning:
        bd["morning_brief"] = ""
    if with_kr_only_extra:
        bd["kr_impact"].append({
            "sector_norm": "shipbuilding_kr",
            "label": "조선 (KR-only)",
            "strength": "upside_expected",
            "korean_picks": [
                {"ticker": "009540", "asset_name": "HD한국조선해양", "market": "KOSPI",
                 "rationale": "수주 모멘텀 지속.", "expected_open_change_pct": "+1~2%"},
            ],
            "catalysts_kr": "",
            "related_etfs": [],
        })

    return {
        "briefing_date": "2026-04-26",
        "source_trade_date": "2026-04-24",
        "status": "success",
        "generated_at": "2026-04-26T06:36:00",
        "updated_at": "2026-04-26T06:36:00",
        "us_summary": {
            "trade_date": "2026-04-24",
            "universe_size": 600,
            "top_movers": [
                {"ticker": "INTC", "asset_name": "Intel Corporation",
                 "market": "NASDAQ", "sector_norm": "semiconductors", "change_pct": 23.60},
                {"ticker": "ARM", "asset_name": "Arm Holdings",
                 "market": "NASDAQ", "sector_norm": "semiconductors", "change_pct": 14.76},
            ],
            "top_losers": [
                {"ticker": "CHTR", "asset_name": "Charter Communications",
                 "market": "NASDAQ", "sector_norm": "communication_cable", "change_pct": -25.50},
            ],
            "sector_aggregates": [
                {
                    "sector_norm": "semiconductors", "label": "반도체",
                    "n": 30, "avg_change_pct": 4.66, "median_change_pct": 3.11,
                    "max_change_pct": 23.60, "min_change_pct": -1.50,
                    "top_stocks": [
                        {"ticker": "INTC", "asset_name": "Intel Corporation",
                         "market": "NASDAQ", "sector_norm": "semiconductors", "change_pct": 23.60},
                        {"ticker": "AMD", "asset_name": "Advanced Micro Devices",
                         "market": "NASDAQ", "sector_norm": "semiconductors", "change_pct": 13.91},
                    ],
                },
                {
                    "sector_norm": "communication_cable", "label": "통신·케이블",
                    "n": 8, "avg_change_pct": -2.94, "median_change_pct": -3.10,
                    "max_change_pct": 0.50, "min_change_pct": -25.50,
                    "top_stocks": [
                        {"ticker": "CHTR", "asset_name": "Charter Communications",
                         "market": "NASDAQ", "sector_norm": "communication_cable", "change_pct": -25.50},
                        {"ticker": "CMCSA", "asset_name": "Comcast Corporation",
                         "market": "NASDAQ", "sector_norm": "communication_cable", "change_pct": -12.90},
                    ],
                },
            ],
            "indices": {},
        },
        "briefing_data": bd,
        "regime_snapshot": {},
        "error_message": None,
    }
```

- [ ] **Step 3: 테스트 helper 모듈 작성 (env stub)**

`tests/test_briefing_template.py`:
```python
"""briefing.html 렌더링 테스트.

base.html 의존을 회피하기 위해 DictLoader로 stub base를 주입한 jinja2 환경에서
briefing.html 만 부분 렌더 → 출력 HTML에 substring assertion.
"""
from __future__ import annotations

import re
from unittest.mock import MagicMock

import pytest
from jinja2 import ChoiceLoader, DictLoader, Environment, FileSystemLoader

from api.template_filters import register
from tests._fixtures.briefing_sample import make_briefing


# base.html 의 최소 stub — content/head block 만 노출
_BASE_STUB = (
    "{% block head %}{% endblock %}\n"
    "<!--CONTENT-->\n"
    "{% block content %}{% endblock %}\n"
    "<!--/CONTENT-->\n"
)


@pytest.fixture
def env():
    e = Environment(
        loader=ChoiceLoader([
            DictLoader({"base.html": _BASE_STUB}),
            FileSystemLoader("api/templates"),
        ]),
        autoescape=True,
    )
    register(e)
    return e


def render(env, **ctx) -> str:
    """briefing.html 을 stub base 위에 렌더해 HTML 문자열 반환."""
    return env.get_template("briefing.html").render(
        request=MagicMock(),
        active_nav="briefing",
        current_user=None,
        auth_enabled=False,
        unread_notifications=0,
        **ctx,
    )


def test_renders_without_briefing_shows_empty_state(env):
    html = render(env, briefing=None, requested_date="2026-04-26")
    assert "브리핑이 없습니다" in html or "준비되지 않았습니다" in html
```

- [ ] **Step 4: 테스트 실행 — 초기 통과 확인 (기존 템플릿 그대로)**

Run: `pytest tests/test_briefing_template.py -v`
Expected: `test_renders_without_briefing_shows_empty_state` PASS (현재 템플릿이 빈 상태 렌더는 이미 처리)

- [ ] **Step 5: 커밋**

```bash
git add tests/_fixtures/__init__.py tests/_fixtures/briefing_sample.py tests/test_briefing_template.py
git commit -m "test(briefing): 템플릿 렌더 테스트 인프라 + 샘플 fixture"
```

---

## Task 2: Hero 카드 — 헤드라인 + morning_brief 통합

**Files:**
- Modify: `api/templates/briefing.html` (line 116~127, 209~215 영역 재구성)
- Modify: `tests/test_briefing_template.py` (테스트 추가)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_briefing_template.py` 끝에 추가:
```python
def test_hero_combines_headline_and_morning_brief(env):
    html = render(env, briefing=make_briefing())
    # headline 과 morning_brief 둘 다 같은 hero 컨테이너 안에 있어야 함
    m = re.search(r'<section[^>]*class="[^"]*brief-hero[^"]*"[^>]*>(.*?)</section>',
                  html, re.DOTALL)
    assert m, "brief-hero section not found"
    body = m.group(1)
    assert "INTC +23.60%" in body
    assert "오늘 챙겨야 할 핵심은 반도체" in body


def test_no_separate_morning_section(env):
    """기존 ③ 모닝 코멘트 섹션 자체가 사라져야 함 — heading 텍스트로 검증."""
    html = render(env, briefing=make_briefing())
    # h3 안에 '모닝 코멘트' 라는 별도 섹션 헤더가 없어야 함
    assert not re.search(r'<h3[^>]*>.*모닝 코멘트.*</h3>', html, re.DOTALL)
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected:
- `test_hero_combines_headline_and_morning_brief` FAIL (`brief-hero` section 없음)
- `test_no_separate_morning_section` FAIL (현재 ③ 모닝 코멘트 섹션 존재)

- [ ] **Step 3: briefing.html 수정 — hero 카드 도입 + ③ 섹션 제거**

[`api/templates/briefing.html`](api/templates/briefing.html) 의 다음 두 영역을 교체:

(A) 기존 라인 ~123-127 (`brief-headline` 단일 줄):
```jinja2
{% if us.get('headline') %}
<div class="brief-headline">
    <strong>📰 {{ us.headline }}</strong>
</div>
{% endif %}
```
→ 다음으로 교체 (헤드라인 + morning_brief 통합 hero):
```jinja2
{% if us.get('headline') or bd.get('morning_brief') %}
<section class="brief-hero">
    {% if us.headline %}
    <div class="brief-hero-headline">📰 <strong>{{ us.headline }}</strong></div>
    {% endif %}
    {% if bd.morning_brief %}
    <div class="brief-hero-body">{{ bd.morning_brief }}</div>
    {% endif %}
</section>
{% endif %}
```

(B) 기존 라인 ~209-215 (`③ 모닝 코멘트` 섹션 전체) → **완전 삭제**:
```jinja2
{# ── 3. 모닝 브리핑 코멘트 ── #}
{% if bd.get('morning_brief') %}
<div class="brief-section">
    <h3><span class="num">③</span> 모닝 코멘트</h3>
    <div class="brief-morning">{{ bd.morning_brief }}</div>
</div>
{% endif %}
```

(C) `<style>` 블록 안에 신규 CSS 추가 (`.brief-headline` 줄 근처):
```css
.brief-hero {
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.08), rgba(34, 211, 238, 0.04));
    border: 1px solid rgba(99, 102, 241, 0.18);
    border-radius: 10px;
    padding: 16px 20px;
    margin-bottom: 18px;
}
.brief-hero-headline {
    font-size: 15.5px;
    line-height: 1.5;
    color: var(--text);
}
.brief-hero-body {
    margin-top: 12px;
    padding-top: 12px;
    border-top: 1px dashed rgba(255, 255, 255, 0.08);
    font-size: 14px;
    line-height: 1.65;
    color: var(--text);
}
```

- [ ] **Step 4: 테스트 실행 — PASS 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected: 모든 테스트 PASS

- [ ] **Step 5: 커밋**

```bash
git add api/templates/briefing.html tests/test_briefing_template.py
git commit -m "feat(briefing): hero 카드로 헤드라인+모닝 코멘트 통합"
```

---

## Task 3: 통합 섹터 카드 — summary 헤더 + 매핑 dict + 펼침 규칙

**Files:**
- Modify: `api/templates/briefing.html` (line ~129-157 ① 섹션 재구성, 매핑 namespace 추가)
- Modify: `tests/test_briefing_template.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_sector_card_uses_details_with_open_when_kr_picks_present(env):
    html = render(env, briefing=make_briefing())
    # 반도체 섹터 — KR picks 있음 → <details open>
    semi = re.search(
        r'<details[^>]*class="[^"]*sector-card[^"]*"[^>]*>\s*'
        r'<summary[^>]*>.*?반도체.*?</summary>',
        html, re.DOTALL,
    )
    assert semi, "sector-card details not found for semiconductors"
    assert "open" in semi.group(0), "semiconductors card should be open by default"


def test_sector_card_collapsed_when_no_kr_match(env):
    html = render(env, briefing=make_briefing())
    # 통신/케이블 섹터 — KR picks 없음 → <details> open 없음
    cable = re.search(
        r'<details(?P<attrs>[^>]*)>\s*'
        r'<summary[^>]*>.*?통신·케이블.*?</summary>',
        html, re.DOTALL,
    )
    assert cable, "sector-card details not found for communication_cable"
    assert "open" not in cable.group("attrs"), \
        "communication_cable card should be collapsed (no KR picks)"


def test_sector_summary_shows_avg_change_and_strength(env):
    html = render(env, briefing=make_briefing())
    # 반도체 카드의 summary 안에 평균 등락 + KR strength 라벨
    m = re.search(
        r'<summary[^>]*>(?P<inner>.*?반도체.*?)</summary>',
        html, re.DOTALL,
    )
    assert m, "summary not found"
    inner = m.group("inner")
    assert "+4.66%" in inner, "avg_change_pct missing/wrong format"
    assert "갭 상승 강력" in inner, "strength label missing"


def test_sector_summary_shows_kr_no_match_when_kr_absent(env):
    html = render(env, briefing=make_briefing())
    m = re.search(
        r'<summary[^>]*>(?P<inner>.*?통신·케이블.*?)</summary>',
        html, re.DOTALL,
    )
    assert m
    assert "KR 매칭 없음" in m.group("inner")
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

Run: `pytest tests/test_briefing_template.py -v -k "sector_card or sector_summary"`
Expected: 4개 모두 FAIL (sector-card details, KR strength label dict 등 미구현)

- [ ] **Step 3: briefing.html — STRENGTH_LABEL dict + namespace 매핑 + summary 헤더 구현**

기존 라인 ~119-121 의 변수 바인딩 영역(`{% set bd ... %}` 다음)에 매핑 코드 삽입:
```jinja2
{% set bd = briefing.briefing_data or {} %}
{% set us = bd.get('us_summary', {}) or {} %}
{% set kr = bd.get('kr_impact', []) or [] %}

{# ── 매핑: sector_norm → kr_impact / sector_aggregates / ticker→asset_name ── #}
{% set STRENGTH_LABEL = {
    'gap_up_strong':   '갭 상승 강력',
    'gap_up_likely':   '갭 상승 유력',
    'upside_expected': '상승 기대',
    'partial_benefit': '보조적 수혜',
    'limited':         '영향 제한적',
} %}

{% set kr_map = namespace(d={}) %}
{% for k in kr %}{% set _ = kr_map.d.update({k.sector_norm: k}) %}{% endfor %}

{% set agg_map = namespace(d={}) %}
{% for s in (briefing.us_summary or {}).get('sector_aggregates', []) %}
    {% set _ = agg_map.d.update({s.sector_norm: s}) %}
{% endfor %}

{% set name_map = namespace(d={}) %}
{% for r in (briefing.us_summary or {}).get('top_movers', []) %}
    {% set _ = name_map.d.update({r.ticker: {'asset_name': r.asset_name, 'market': r.market}}) %}
{% endfor %}
{% for r in (briefing.us_summary or {}).get('top_losers', []) %}
    {% set _ = name_map.d.update({r.ticker: {'asset_name': r.asset_name, 'market': r.market}}) %}
{% endfor %}
{% for s in (briefing.us_summary or {}).get('sector_aggregates', []) %}
    {% for r in s.get('top_stocks', []) %}
        {% set _ = name_map.d.update({r.ticker: {'asset_name': r.asset_name, 'market': r.market}}) %}
    {% endfor %}
{% endfor %}
```

기존 라인 ~129-157 (`① 미국 시장 핵심 상승 섹터` 영역 전체)를 다음으로 교체:
```jinja2
{# ── ① 섹터별 흐름 (US ↔ KR 통합) ── #}
{% if us.get('groups') or kr %}
<div class="brief-section">
    <h3><span class="num">①</span> 섹터별 흐름 (US ↔ KR 통합)</h3>

    {% for g in us.get('groups', []) %}
        {% set kr_grp = kr_map.d.get(g.sector_norm) %}
        {% set agg = agg_map.d.get(g.sector_norm) %}
        {% set has_kr = kr_grp and (kr_grp.korean_picks | length) > 0 %}
        {% set icon = '🔵' if loop.index == 1 else
                      '⚡' if loop.index == 2 else
                      '🍎' if loop.index == 3 else
                      '💊' if loop.index == 4 else '📈' %}

        <details class="sector-card"{% if has_kr %} open{% endif %}>
            <summary class="sector-summary">
                <span class="sector-icon">{{ icon }}</span>
                <span class="sector-label">{{ g.label or g.sector_norm }}</span>

                {% if agg and agg.avg_change_pct is not none %}
                    <span class="sector-mean {{ 'up' if agg.avg_change_pct >= 0 else 'dn' }}">
                        평균 {{ '%+.2f' % agg.avg_change_pct }}%
                    </span>
                {% endif %}

                {% if kr_grp and kr_grp.strength %}
                    <span class="strength-badge strength-{{ kr_grp.strength }}">
                        {{ STRENGTH_LABEL.get(kr_grp.strength, kr_grp.strength) }}
                    </span>
                {% else %}
                    <span class="strength-badge strength-none">KR 매칭 없음</span>
                {% endif %}

                <span class="sector-chevron" aria-hidden="true">▾</span>
            </summary>

            <div class="sector-card-body">
                {# Task 4 에서 본문 채움 (US movers + KR picks) — 임시 자리표시 #}
            </div>
        </details>
    {% endfor %}
</div>
{% endif %}
```

`<style>` 블록에 CSS 추가:
```css
.sector-card {
    border: 1px solid var(--border);
    border-radius: 10px;
    margin-bottom: 12px;
    background: var(--bg-card, var(--bg-elevated));
    overflow: hidden;
}
.sector-card > summary {
    list-style: none;
    cursor: pointer;
    padding: 12px 16px;
}
.sector-card > summary::-webkit-details-marker { display: none; }
.sector-summary {
    display: flex; align-items: center; flex-wrap: wrap; gap: 10px;
}
.sector-icon { font-size: 18px; }
.sector-label { font-weight: 600; font-size: 15px; flex: 1 1 auto; }
.sector-mean { font-size: 13px; padding: 2px 8px; border-radius: 6px;
               background: var(--bg); border: 1px solid var(--border); }
.sector-mean.up { color: #2ecc71; }
.sector-mean.dn { color: #e74c3c; }
.strength-none { background: rgba(255, 255, 255, 0.04); color: #6b7280;
                 padding: 3px 10px; border-radius: 6px; font-size: 12px; font-weight: 600; }
.sector-chevron { color: var(--text-muted); font-size: 14px; transition: transform .2s; }
.sector-card[open] .sector-chevron { transform: rotate(180deg); }
.sector-card-body { padding: 0 16px 14px; }
```

- [ ] **Step 4: 테스트 실행 — PASS 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected: Task 1·2 테스트 + Task 3 sector_card/sector_summary 테스트 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add api/templates/briefing.html tests/test_briefing_template.py
git commit -m "feat(briefing): 통합 섹터 카드 헤더 + sector_norm 매핑 dict"
```

---

## Task 4: 통합 섹터 카드 본문 — US movers + catalyst + KR picks

**Files:**
- Modify: `api/templates/briefing.html` (Task 3 의 임시 자리표시 영역 채움)
- Modify: `tests/test_briefing_template.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_sector_card_body_has_us_movers_and_kr_picks(env):
    html = render(env, briefing=make_briefing())
    # 반도체 카드 본문 추출
    m = re.search(
        r'<details[^>]*sector-card[^>]*>.*?반도체.*?</summary>'
        r'(?P<body>.*?)</details>',
        html, re.DOTALL,
    )
    assert m, "sector-card not found"
    body = m.group("body")
    # US movers
    assert "INTC" in body and "+23.60%" in body
    assert "ARM" in body and "+14.76%" in body
    # catalyst
    assert "INTC 단일 종목 +23.60% 급등" in body
    # KR picks
    assert "SK하이닉스" in body
    assert "한미반도체" in body
    assert "+2~4%" in body


def test_us_only_sector_shows_no_kr_match_message(env):
    html = render(env, briefing=make_briefing())
    m = re.search(
        r'<details[^>]*sector-card[^>]*>.*?통신·케이블.*?</summary>'
        r'(?P<body>.*?)</details>',
        html, re.DOTALL,
    )
    assert m
    body = m.group("body")
    assert "CHTR" in body and "-25.50%" in body
    # KR 매칭 없는 카드는 한국 종목 영역 자체가 한 줄짜리 안내로
    assert ("한국 매칭 없음" in body) or ("영향 제한적" in body)
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

Run: `pytest tests/test_briefing_template.py -v -k "sector_card_body or us_only"`
Expected: 2개 FAIL (현재 본문은 자리표시만)

- [ ] **Step 3: briefing.html — 카드 본문 구현**

Task 3 의 `<div class="sector-card-body">` 내부 자리표시를 다음으로 교체:
```jinja2
<div class="sector-card-body">
    {# US block — movers + catalyst #}
    <div class="us-block">
        <div class="us-block-title">🇺🇸 US Movers</div>
        <div class="movers-row">
            {% for m in g.get('top_movers', []) %}
                {% set info = name_map.d.get(m.ticker, {}) %}
                {% set fullname = info.asset_name or m.ticker %}
                {% set mkt = info.market or '' %}
                <a class="mover-pill"
                   href="/pages/stocks/{{ m.ticker }}{% if mkt %}?market={{ mkt }}{% endif %}"
                   title="{{ fullname }}{% if mkt %} · {{ mkt }}{% endif %}">
                    <strong>{{ m.ticker }}</strong>
                    <span class="{{ 'mover-pos' if (m.change_pct or 0) >= 0 else 'mover-neg' }}">
                        {{ '%+.2f' % (m.change_pct or 0) }}%
                    </span>
                </a>
            {% endfor %}
        </div>
        {% if g.catalyst %}
        <div class="brief-catalyst">📌 {{ g.catalyst }}</div>
        {% endif %}
    </div>

    {# KR block #}
    {% if has_kr %}
    <div class="kr-block">
        <div class="kr-block-title">🇰🇷 KR Picks ({{ kr_grp.korean_picks | length }}종목)</div>
        {% for p in kr_grp.korean_picks %}
        <div class="kr-pick">
            <div>
                <div class="kr-pick-name">{{ p.asset_name }}</div>
                <div class="kr-pick-ticker">{{ p.ticker }} · {{ p.market }}</div>
            </div>
            <div class="kr-pick-rationale">{{ p.rationale }}</div>
            {% if p.expected_open_change_pct %}
            <div class="kr-pick-gap {% if '-' in p.expected_open_change_pct %}kr-pick-gap-loss{% endif %}">
                {{ p.expected_open_change_pct }}
            </div>
            {% endif %}
        </div>
        {% endfor %}
        {% if kr_grp.catalysts_kr %}
        <div class="brief-catalyst" style="margin-top:10px;">📌 {{ kr_grp.catalysts_kr }}</div>
        {% endif %}
        {% if kr_grp.related_etfs %}
        <div class="related-etfs">
            관련:
            {% for etf in kr_grp.related_etfs %}<span>{{ etf }}</span>{% endfor %}
        </div>
        {% endif %}
    </div>
    {% else %}
    <div class="kr-block kr-block-empty">
        🇰🇷 한국 매칭 없음 · 영향 제한적
    </div>
    {% endif %}
</div>
```

`<style>` 추가:
```css
.us-block { padding-top: 6px; }
.kr-block { margin-top: 14px; padding-top: 12px; border-top: 1px dashed rgba(255,255,255,0.06); }
.kr-block-empty { color: var(--text-muted); font-size: 13px; }
.us-block-title, .kr-block-title { font-size: 12.5px; color: var(--text-muted);
                                    margin-bottom: 8px; font-weight: 600; letter-spacing: .03em; }
```

- [ ] **Step 4: 테스트 실행 — PASS 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected: 모든 테스트 PASS

- [ ] **Step 5: 커밋**

```bash
git add api/templates/briefing.html tests/test_briefing_template.py
git commit -m "feat(briefing): 통합 섹터 카드 본문 — US movers + KR picks"
```

---

## Task 5: US mover pill — `<a>` 풀네임 툴팁 + cockpit 링크 검증

> Task 4 에서 이미 `<a>` 로 마크업했으므로 이 task 는 **검증/회귀 테스트 + CSS 마감**.

**Files:**
- Modify: `api/templates/briefing.html` (CSS만 — `.mover-pill` 의 `<a>` 스타일)
- Modify: `tests/test_briefing_template.py`

- [ ] **Step 1: 추가 테스트 — pill 링크/title 정확성**

```python
def test_mover_pill_is_anchor_with_cockpit_link(env):
    html = render(env, briefing=make_briefing())
    # INTC pill 이 <a href="/pages/stocks/INTC?market=NASDAQ" title="Intel Corporation · NASDAQ">
    pill = re.search(
        r'<a class="mover-pill"\s+href="([^"]+)"\s+title="([^"]+)"\s*>'
        r'\s*<strong>INTC</strong>',
        html,
    )
    assert pill, "INTC mover pill anchor not found"
    href, title = pill.group(1), pill.group(2)
    assert href == "/pages/stocks/INTC?market=NASDAQ"
    assert "Intel Corporation" in title
    assert "NASDAQ" in title


def test_mover_pill_falls_back_to_ticker_when_no_name_map_hit(env):
    """name_map 에 없는 티커는 풀네임 폴백 + market 쿼리 없이 링크."""
    bd = make_briefing()
    # 매핑에 없는 ticker 추가
    bd["briefing_data"]["us_summary"]["groups"][0]["top_movers"].append(
        {"ticker": "ZZZZ", "change_pct": 1.0}
    )
    html = render(env, briefing=bd)
    pill = re.search(
        r'<a class="mover-pill"\s+href="([^"]+)"\s+title="([^"]+)"\s*>'
        r'\s*<strong>ZZZZ</strong>',
        html,
    )
    assert pill
    assert pill.group(1) == "/pages/stocks/ZZZZ"  # market 없음
    assert pill.group(2).startswith("ZZZZ")        # 풀네임 폴백
```

- [ ] **Step 2: 테스트 실행 — PASS 기대 (Task 4 구현으로 이미 충족)**

Run: `pytest tests/test_briefing_template.py -v -k "mover_pill"`
Expected: 둘 다 PASS

- [ ] **Step 3: `.mover-pill` CSS 마감 — `<a>` 스타일**

기존 `.mover-pill` 정의를 다음으로 교체 (텍스트 데코·색 상속):
```css
.mover-pill {
    display: inline-flex; gap: 4px; align-items: center;
    padding: 2px 8px; background: var(--bg);
    border: 1px solid var(--border); border-radius: 14px;
    text-decoration: none; color: inherit;
    transition: background .15s, border-color .15s;
}
.mover-pill:hover {
    background: var(--bg-elevated);
    border-color: var(--accent);
}
```

- [ ] **Step 4: 테스트 재실행 — 회귀 없음 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected: 전체 PASS

- [ ] **Step 5: 커밋**

```bash
git add api/templates/briefing.html tests/test_briefing_template.py
git commit -m "feat(briefing): US mover pill 풀네임 툴팁 + 종목상세 링크"
```

---

## Task 6: KR-only 섹터 fallback

**Files:**
- Modify: `api/templates/briefing.html` (① 섹션 끝에 KR-only 루프 추가)
- Modify: `tests/test_briefing_template.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
def test_kr_only_sector_renders_after_us_groups(env):
    """US groups 에 없는 sector_norm 의 kr_impact 도 표시되어야 함."""
    bd = make_briefing(with_kr_only_extra=True)  # shipbuilding_kr 추가
    html = render(env, briefing=bd)
    # HD한국조선해양 + 조선 라벨 등장
    assert "HD한국조선해양" in html
    assert "조선" in html


def test_kr_only_sector_card_is_open_by_default(env):
    """KR-only 카드는 KR picks 가 정의상 ≥1 이므로 기본 펼침."""
    bd = make_briefing(with_kr_only_extra=True)
    html = render(env, briefing=bd)
    m = re.search(
        r'<details(?P<attrs>[^>]*sector-card[^>]*)>\s*'
        r'<summary[^>]*>.*?조선.*?</summary>',
        html, re.DOTALL,
    )
    assert m, "KR-only sector card not found"
    assert "open" in m.group("attrs")
```

- [ ] **Step 2: FAIL 확인**

Run: `pytest tests/test_briefing_template.py -v -k "kr_only"`
Expected: 2개 FAIL

- [ ] **Step 3: briefing.html — KR-only fallback 루프 추가**

① 섹션의 US `{% for g in us.get('groups', []) %}{% endfor %}` 닫힌 직후, `</div>` 로 brief-section 닫기 전에 추가:
```jinja2
{# KR-only — US groups 에 없는 sector_norm #}
{% set us_keys = us.get('groups', []) | map(attribute='sector_norm') | list %}
{% for k in kr if k.sector_norm not in us_keys %}
    {% set has_kr = (k.korean_picks | length) > 0 %}
    <details class="sector-card sector-card-kr-only"{% if has_kr %} open{% endif %}>
        <summary class="sector-summary">
            <span class="sector-icon">🇰🇷</span>
            <span class="sector-label">{{ k.label or k.sector_norm }}</span>
            {% if k.strength %}
            <span class="strength-badge strength-{{ k.strength }}">
                {{ STRENGTH_LABEL.get(k.strength, k.strength) }}
            </span>
            {% endif %}
            <span class="sector-chevron" aria-hidden="true">▾</span>
        </summary>
        <div class="sector-card-body">
            <div class="kr-block">
                <div class="kr-block-title">🇰🇷 KR Picks ({{ k.korean_picks | length }}종목) · US 직접 매칭 없음</div>
                {% for p in k.korean_picks %}
                <div class="kr-pick">
                    <div>
                        <div class="kr-pick-name">{{ p.asset_name }}</div>
                        <div class="kr-pick-ticker">{{ p.ticker }} · {{ p.market }}</div>
                    </div>
                    <div class="kr-pick-rationale">{{ p.rationale }}</div>
                    {% if p.expected_open_change_pct %}
                    <div class="kr-pick-gap {% if '-' in p.expected_open_change_pct %}kr-pick-gap-loss{% endif %}">
                        {{ p.expected_open_change_pct }}
                    </div>
                    {% endif %}
                </div>
                {% endfor %}
                {% if k.catalysts_kr %}
                <div class="brief-catalyst" style="margin-top:10px;">📌 {{ k.catalysts_kr }}</div>
                {% endif %}
                {% if k.related_etfs %}
                <div class="related-etfs">
                    관련:
                    {% for etf in k.related_etfs %}<span>{{ etf }}</span>{% endfor %}
                </div>
                {% endif %}
            </div>
        </div>
    </details>
{% endfor %}
```

- [ ] **Step 4: PASS 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected: 전체 PASS

- [ ] **Step 5: 커밋**

```bash
git add api/templates/briefing.html tests/test_briefing_template.py
git commit -m "feat(briefing): KR-only 섹터 fallback 카드"
```

---

## Task 7: 모바일 미디어 쿼리 + 데드 CSS 정리

**Files:**
- Modify: `api/templates/briefing.html`

- [ ] **Step 1: 모바일 reflow 미디어 쿼리 추가**

`<style>` 블록 끝의 기존 `@media (max-width: 768px)` 쿼리에 다음 룰 추가 (있는 룰은 그대로 유지):
```css
@media (max-width: 768px) {
    .kr-pick { grid-template-columns: 1fr; gap: 4px; }
    .kr-pick-gap { justify-self: start; }

    .sector-summary { gap: 6px; }
    .sector-label { flex: 1 1 100%; order: -1; }
    .sector-mean, .strength-badge, .strength-none { font-size: 11.5px; padding: 2px 6px; }
    .sector-chevron { margin-left: auto; }
}
```

- [ ] **Step 2: 데드 CSS 정리 — 사용 안 되는 셀렉터 주석 처리**

다음 셀렉터들은 새 마크업이 사용하지 않음. **삭제하지 말고 한 줄 주석만** 달아 후속 정리 가능하게 표시:
- `.us-group`, `.us-group-header`, `.us-group-title`, `.us-group-title .icon`
- `.kr-group`, `.kr-group-header`, `.kr-group-title`, `.kr-group-title .icon`
- `.brief-headline`
- `.brief-morning`

각 정의 윗줄에:
```css
/* deprecated v2 — 새 마크업 미사용. 다른 곳 참조 없으면 후속 PR 에서 제거. */
.us-group { ... }
```

- [ ] **Step 3: 테스트 실행 — 회귀 없음 확인**

Run: `pytest tests/test_briefing_template.py -v`
Expected: 전체 PASS

- [ ] **Step 4: 커밋**

```bash
git add api/templates/briefing.html
git commit -m "style(briefing): 모바일 reflow + 데드 CSS deprecated 마킹"
```

---

## Task 8: 수동 시각 검증 + 회귀 스모크 + 마무리 커밋

**Files:**
- 없음 (검증만)

- [ ] **Step 1: 전체 테스트 스위트 통과 확인**

Run: `pytest -v`
Expected: 기존 테스트 + 신규 `test_briefing_template.py` 모두 PASS, 회귀 0

- [ ] **Step 2: 로컬 dev 서버 띄우고 실제 데이터로 시각 확인**

```bash
python -m api.main
# 또는: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
```

브라우저에서 `http://localhost:8000/pages/briefing` 열고 다음을 시각 확인 (수용 기준 § 10 기준):

| 검증 항목 | 기대 |
|---|---|
| 페이지 최상단 hero 카드 | 헤드라인(굵게) + morning_brief(서술) 한 카드 안에 |
| ③ 모닝 코멘트 섹션 | **존재하지 않음** |
| 섹터 카드 — KR picks 있음 | 펼쳐진 채 진입, US movers + KR picks 둘 다 보임 |
| 섹터 카드 — KR picks 없음 | 헤더만 보이고 접힘. 클릭 시 펼침 |
| US mover pill hover | 풀네임 툴팁 (예: "Intel Corporation · NASDAQ") |
| US mover pill click | `/pages/stocks/INTC?market=NASDAQ` 로 이동 |
| 평균 등락 배지 | 헤더에 `평균 +N.NN%` 표시 (sector_aggregates.avg_change_pct) |
| KR strength badge | 기존 5종 색상 + 신규 회색 "KR 매칭 없음" |
| 모바일 (개발자도구 ≤768px) | 카드 헤더 1열, KR pick 1열 reflow, 가로 overflow 없음 |
| 시장 레짐 디버그 섹션 | 그대로 표시 |
| 빈 브리핑 페이지 (`/pages/briefing/2099-01-01`) | 빈 상태 메시지 표시 |

- [ ] **Step 3: 검증 결과 캡처(선택) — 이슈 발견 시**

문제 발견 시 해당 task 로 돌아가 fix → 테스트 추가 → 재커밋. 정상이면 다음 단계.

- [ ] **Step 4: prompts 파일 staging + 마무리 커밋**

CLAUDE.md 컨벤션: 작업 commit 에 conversation prompt 기록 함께 묶기.
```bash
git add _docs/_prompts/20260426_prompt.md _docs/20260426120614_briefing-v2-design.md _docs/20260426121156_briefing-v2-plan.md
git commit -m "docs(briefing): v2 디자인·구현 플랜 + 작업 프롬프트 기록"
```

---

## 후속 (out-of-scope, 메모)

- 데드 CSS (`.us-group`, `.kr-group`, `.brief-headline`, `.brief-morning`) — 다른 라우트 참조 없는 것 확인 후 별도 PR 에서 완전 삭제
- 평균 등락 외 max/min mover 강조
- KR pick row 클릭 시 종목 상세 페이지 (현재 텍스트만)
- mover pill sector_norm 컬러 코딩

# OAuth(Google + Kakao) 운영 세팅 가이드

설계 spec: [20260525144745_oauth-google-kakao-design.md](20260525144745_oauth-google-kakao-design.md)

이 문서는 코드 작업 외 인프라/콘솔 등록 절차를 다룬다.

---

## 1. Cloudflare Tunnel (라파 운영기 HTTPS 노출)

OAuth 운영을 위해서는 도메인 + HTTPS 가 필수다 (Google/Kakao 양쪽 정책).
Cloudflare Tunnel 을 사용하면 공유기 포트포워딩 없이 무료로 가능하다.

### 1.1 cloudflared 설치 (라파)

```bash
curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64.deb
sudo dpkg -i cloudflared.deb
cloudflared --version
```

### 1.2 터널 생성

```bash
cloudflared tunnel login
cloudflared tunnel create investment-advisor
cloudflared tunnel route dns investment-advisor <your-subdomain>.<your-domain>
```

### 1.3 config.yml

`~/.cloudflared/config.yml` (또는 `/etc/cloudflared/config.yml`) 작성:

```yaml
tunnel: <tunnel-id>
credentials-file: /home/dzp/.cloudflared/<tunnel-id>.json

ingress:
  - hostname: <your-subdomain>.<your-domain>
    service: http://localhost:8000
  - service: http_status:404
```

### 1.4 systemd 등록

```bash
sudo cloudflared service install
sudo systemctl enable --now cloudflared
```

상태 확인: `sudo systemctl status cloudflared`

---

## 2. Google Cloud Console

1. https://console.cloud.google.com → 프로젝트 생성/선택
2. "API 및 서비스" → "OAuth 동의 화면" → 외부(External) 선택 → 앱 이름/이메일 입력
3. "사용자 인증 정보" → "+ 사용자 인증 정보 만들기" → "OAuth 클라이언트 ID" → 웹 애플리케이션
4. "승인된 리디렉션 URI" 2개 등록:
   - `http://localhost:8000/auth/oauth/google/callback`
   - `https://<your-subdomain>.<your-domain>/auth/oauth/google/callback`
5. 생성 후 Client ID / Client Secret 메모 → `.env` 의 `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`

> **주의**: 리디렉션 URI 는 trailing slash, http/https, 포트 모두 character-for-character 일치해야 한다. 콘솔 저장 후 몇 분 지연될 수 있음.

---

## 3. Kakao Developers

1. https://developers.kakao.com → 애플리케이션 추가
2. "플랫폼" → Web → 사이트 도메인 등록:
   - `http://localhost:8000`
   - `https://<your-subdomain>.<your-domain>`
3. "카카오 로그인" → 활성화 ON
4. "카카오 로그인" → "Redirect URI" 등록:
   - `http://localhost:8000/auth/oauth/kakao/callback`
   - `https://<your-subdomain>.<your-domain>/auth/oauth/kakao/callback`
5. "동의항목" → "카카오계정(이메일)" → **필수 동의** 설정
6. **"비즈앱 신청"** — 이메일 필수 동의는 비즈앱 전환 후에만 가능. 개인 비즈앱은 사업자등록 없이 신청 가능, 심사 1~3 영업일.
7. "앱 키" → "REST API 키" 메모 → `.env` 의 `KAKAO_CLIENT_ID`

> **주의**: 비즈앱 전환 없이 이메일 필수 동의 설정 시 저장 불가. 이메일 없는 Kakao 계정 로그인 시도는 콜백에서 `email_required` 에러 반환.

---

## 4. .env 운영 값 예시

```ini
OAUTH_ENABLED=true
OAUTH_SESSION_SECRET=<python -c "import secrets; print(secrets.token_hex(32))">

GOOGLE_CLIENT_ID=<Google Cloud Console>
GOOGLE_CLIENT_SECRET=<Google Cloud Console>
GOOGLE_REDIRECT_URI=https://<subdomain>.<domain>/auth/oauth/google/callback

KAKAO_CLIENT_ID=<Kakao REST API 키>
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=https://<subdomain>.<domain>/auth/oauth/kakao/callback
```

> `OAUTH_SESSION_SECRET` 은 반드시 무작위 64자 이상 hex 값으로 설정. 빈 값이면 startup 에서 경고 로그 후 OAuth 기능 비활성화.

---

## 5. 의존성 설치

```bash
pip install authlib httpx itsdangerous
# 또는 requirements.txt 에 이미 포함된 경우 생략
```

---

## 6. 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| `redirect_uri_mismatch` (Google) | 콘솔 등록값과 .env 불일치 | trailing slash, http/https, 포트 모두 character-for-character 일치 확인 |
| `KOE006` (Kakao) | Redirect URI 미등록 또는 오타 | Kakao 콘솔에서 등록 URI 와 콜백 URL 비교 |
| Kakao 이메일 동의 화면 안 나옴 | 비즈앱 미전환 또는 필수 동의 미설정 | 비즈앱 신청 + 동의항목 "카카오계정(이메일)" 필수로 변경 |
| 로컬에서 OAuth hang | `SessionMiddleware` 미등록 | `OAUTH_ENABLED=true` + `OAUTH_SESSION_SECRET` 설정 확인 |
| 콜백 `state mismatch` | 동일 브라우저 두 탭 동시 로그인 시도 | 한 탭으로 재시도 |
| `소셜 로그인 계정은 비밀번호가 없습니다` | OAuth-only 유저가 change-password 접근 | 정상 동작 — 비밀번호 설정 기능 추후 제공 예정 |
| cloudflared: `502 Bad Gateway` | API 서버가 아직 기동 안 됨 | `sudo systemctl start investment-advisor-api.service` 후 재시도 |

========================

현재 코드의 주식 분석 로직에 D:\dzp\바이브코딩\_custom_agent\stock_analyst_agents\ 의 에이전트를 사용하도록 하는 방법을 제안해줘.

[추가1] "권장: 방안 2 (SDK 포팅)" 으로 진행해주는데, 현재 로직에서 저장되는 항목과 로직을 개선할 수 있는 부분을 제안하고, db도 그에 맞게 마이그레이션해줘.

[추가2] 변경된 db 마이그레이션 정보에 따라서 프론프엔드 코드도 변경된거야?
 [추가2-1] 어 프론트엔드도 업데이트 해줘.

========================

/init

========================

아래의 프롬프트를 개선해줘.

하루에 한번 씩, 투자 분석을 하면 동일하게 진행 중인 이슈 및 테마정보가 누적되서 db에 저장되는데,
수집된 일자 별로 나눠서 확인이 가능하도록 해주고,
프론트엔드에서 좀더 투자결정을 내리기 좋도록 ui,ux를 변경하는 부분을 검토해서 제안해줘.

[추가1] 제안해준대로 순서대로 수정을 진행해주고, 프론트엔드의 글씨도 좀더 크게 가독성 좋도록 변경해줘.

========================

shared\config.py 의 db접속정보를 .env 파일로 설정해서 사용하도록 추가작업 해줘.

[추가1] .env 는 .gitignore에 포함되어 github에 안올라가는데,, 예제 파일을 설정하는 방법을 알려줘 예).env.example 등..

========================

CLAUDE.md, README.md 의 문서를 갱신해줘.

========================

현재의 프로젝트를 github에 처음 올리는 방법을 정리해서 알려줘.

========================
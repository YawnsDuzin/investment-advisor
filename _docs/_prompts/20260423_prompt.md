========================
2026.04.23(목)
========================

_docs\20260422235016_ohlcv-history-table-plan.md 의 문서의 계획을 확인하고 작업을 진행해줘.

[추가1]
Q1. §3 + §6.1 결정사항 : 기본 제안대로 확정
Q2. 이번 세션 스코프 : D안: 전체 (§6.4 1~7번 모두 + systemd unit 파일 작성)
Q3. 기타 확인사항
- feature/ohlcv-history 새 브랜치에서 작업진행해줘.
- 백필 실행 : 이 부분은 방법만 안내.. 사용자가 직접 실행

[추가1-1] 현재 종목 별 1년치만 수집하도록 구현하는거지?
[추가1-1-1] 1년 이면, 일반적인 분석을 모두 가능할 정도의 데이터 양인지,, 추가로 더 수집하는게 좋을지 제안해줘.
[추가1-1-2] "B. 2년 (추천)" 으로 작업 진행해줘.
[추가1-1-3] 추천하는 "분리유지 "방법으로 진행해줘.

[추가2] deploy\systemd 에 기존의 분석 등의 작업도 추가해줘.

========================

현재 코드가 orm 을 사용해서 구현되어 있는지 확인해서 알려줘.

[추가1] orm을 사용하는 방법으로 변경하는게 효율적일지?? 작업량은 어느정도 일지 확인해서 알려줘.

========================

[[사용자 수동 실행 안내 (라즈베리파이)

# 1. 마이그레이션 적용
python -c "from shared.config import AppConfig; from shared.db import init_db; init_db(AppConfig().db)"

# 2. 초기 800일 백필 (~25분: KRX 20분 + US 5분)
python -m analyzer.universe_sync --mode backfill --days 800 --market KRX
python -m analyzer.universe_sync --mode backfill --days 800 --market US

# 3. 무결성 검사
python -m tools.ohlcv_health_check

# 4. systemd 타이머 활성화 (deploy/systemd/README.md 참고)

========================

git commit & push 까지 처리해줘.

========================

라즈베리파이에서 터미널에서 아래의 작업이 정상 실행되고 있는지 확인하는 
postgresql 스크립트를 알려줘.

# 1. 마이그레이션 적용
python -c "from shared.config import AppConfig; from shared.db import init_db; init_db(AppConfig().db)"

# 2. 초기 800일 백필 (~25분: KRX 20분 + US 5분)
python -m analyzer.universe_sync --mode backfill --days 800 --market KRX
python -m analyzer.universe_sync --mode backfill --days 800 --market US


[추가1] 현재 답변 내용 _docs 폴더에 md 파일로 생성해줘.

========================

(venv) dzp@rasp-dzp-1:~/dzp-main/program/investment-advisor $ python -m analyzer.universe_sync --mode backfill --days 800 --mar
KRX 로그인 시도...
  로그인 ID: yawnsduzin
KRX 로그인 완료.
  로그인 시간: 2026-04-23 10:33:10
  만료 시간: 2026-04-23 11:33:10
Traceback (most recent call last):
  File "<frozen runpy>", line 198, in _run_module_as_main
  File "<frozen runpy>", line 88, in _run_code
  File "/home/dzp/dzp-main/program/investment-advisor/analyzer/universe_sync.py", line 1417, in <module>
    sys.exit(main())
             ^^^^^^
  File "/home/dzp/dzp-main/program/investment-advisor/analyzer/universe_sync.py", line 1361, in main
    _log.info(f"결과: {_run_mode_backfill(cfg, args, krx_markets, us_filter)}")
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/dzp/dzp-main/program/investment-advisor/analyzer/universe_sync.py", line 1326, in _run_mode_                     backfill
    recompute_change_pct(cfg.db)
  File "/home/dzp/dzp-main/program/investment-advisor/analyzer/universe_sync.py", line 1070, in recompute_                     change_pct
    cur.execute(sql)
psycopg2.errors.NumericValueOutOfRange: 오류:  수치 필드 오버플로우
DETAIL:  전체 자릿수 7, 소수 자릿수 4의 필드는 10^3보다 작은 절대 값으로 반올림해야 합니다.

[추가1] 지금과 같은 종목의 데이터가 다른 로직에는 영향이 없을지 검토해줘.
[추가1-1] 그리고 이런 문제가 발생했을때, 종료하지말고, 로그에서 확인가능하도록 예외처리 항목 추가해줘.
[추가1-2] v28 마이그레이션 진행해주고, 이런 문제가 발생했을때, 종료하지말고, 로그에서 확인가능하도록 예외처리 항목 추가해줘.

[[작업
cd ~/dzp-main/program/investment-advisor
git pull origin main
source venv/bin/activate
python -c "from shared.db.schema import init_db; from shared.config import AppConfig; init_db(AppConfig().db)"
# 2. 초기 800일 백필 (~25분: KRX 20분 + US 5분)
python -m analyzer.universe_sync --mode backfill --days 800 --market KRX
python -m analyzer.universe_sync --mode backfill --days 800 --market US
========================

git commit & push 까지 처리해줘.

========================

(venv) dzp@rasp-dzp-1:~/dzp-main/program/investment-advisor $ python -m analyzer.universe_sync --mode backfill --days 800 --market US
KRX 로그인 시도...
  로그인 ID: yawnsduzin
KRX 로그인 완료.
  로그인 시간: 2026-04-23 12:33:10
  만료 시간: 2026-04-23 13:33:10
Failed to get ticker 'REGN' reason: Failed to perform, curl: (7) Failed to connect to query2.finance.yahoo.com port 443 after 73 ms: Co                                                uld not connect to server. See https://curl.se/libcurl/c/libcurl-errors.html first for more details.
Failed to get ticker 'PYPL' reason: Failed to perform, curl: (7) Failed to connect to query2.finance.yahoo.com port 443 after 254 ms: C                                                ould not connect to server. See https://curl.se/libcurl/c/libcurl-errors.html first for more details.

2 Failed downloads:
['REGN', 'PYPL']: ConnectionError('Failed to perform, curl: (7) Failed to connect to query2.finance.yahoo.com port 443 after 0 ms: Coul                                                d not connect to server. See https://curl.se/libcurl/c/libcurl-errors.html first for more details.')

[추가1] 실패 티커만 재시도하는 부분 백필 한 번 돌리기
[[답변

python -m analyzer.universe_sync --mode backfill --days 800 --ticker REGN
python -m analyzer.universe_sync --mode backfill --days 800 --ticker PYPL

[추가1] 아래와 같이 정상 실행 됐으면.. 기존 로직에 추가로 보완이 필요한 부분이 있을지 검토해줘.

(venv) dzp@rasp-dzp-1:~/dzp-main/program/investment-advisor $ python -m analyzer.universe_sync --mode backfill --days 800 --ticker REGN
KRX 로그인 시도...
  로그인 ID: yawnsduzin
KRX 로그인 완료.
  로그인 시간: 2026-04-23 12:45:03
  만료 시간: 2026-04-23 13:45:03
(venv) dzp@rasp-dzp-1:~/dzp-main/program/investment-advisor $ python -m analyzer.universe_sync --mode backfill --days 800 --ticker PYPL
KRX 로그인 시도...
  로그인 ID: yawnsduzin
KRX 로그인 완료.
  로그인 시간: 2026-04-23 12:46:36
  만료 시간: 2026-04-23 13:46:36

[추가1-2] A만 구현해줘.

========================

stock_universe_ohlcv 가 수집되어 있으면, 다음 분석부터 이 데이터를 감안해서 분석이 진행되는거야?

[추가1] 분석시 로직에 포함해서 신뢰성 및 경쟁력을 올릴수 있는 부분과 UI에서 확인 및 활용 가능한 부분을 제안해줘.
[추가1-1] 제안해준 부분을 _docs 폴더에 우선순위를 나눠서 todo 리스트 형식의 md파일로 생성해줘.

[추가2] 현재 브랜치 commit & push 하고, 리모트 최신 정보로 pull 하고 dev 브랜치로 이동해줘.

[추가3] 우선 순위에 맞게 작업 진행해주고, 작업완료 된 사항은 문서에 완료여부 체크하면서 진행해줘.
[추가4] "A2 — 스크리너 이력 필터 확장" 작업 이어서 진행해줘.
[추가5] A3 — Post-Return 추적을 OHLCV로 통합. 범위: price_tracker.py 리팩터 + Max Drawdown/Alpha 등 신규 메트릭 + 필요 시 v29 마이그레이션(선택) 작업 이어서 진행해줘.

========================
========================
========================
========================
========================

git push 하고 pr 까지 생성해줘.

========================
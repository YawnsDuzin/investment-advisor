========================
2026.05.13(수)
========================

[[라즈베리파이 배포:
cd /home/dzp/dzp-main/program/investment-advisor
source venv/bin/activate
git pull origin main
sudo systemctl restart investment-advisor-api

========================

아래는 프리마켓 브리핑 서비스 오류 로그야. 확인해줘.

[[답변
KRX 로그인 완료. 가 안 찍히고 끝났다. pykrx 1.2.7+ KRX 로그인 단계에서 빈 응답을 받아 json.loads("") 가 터진 것.

========================

아래는 "일일 분석 배치" 서비스 오류 로그야. 확인해줘.

========================

웹페이지에서 프리마켓 브리핑 실행 시 첨부이미지와 같은 팝업오류창이 표시되고 있어. 확인해줘.

[추가1] 혹시 동일날짜에 재실행하면 처리가 안되는 로직이있어?
[추가2] data.krx.co.kr 정상적으로 로그인되고, 라즈베리파이 버전은 아래와 같아
[추가3] sudo systemctl restart investment-advisor-api 로 서비스를 재시작하고, 다시 실행하니까 정상적으로 처리되고 있어.. 문제되는 부분을 검토해줘.
[추가3-1] "(b) 1번 + 2번" 추천방법으로 진행해줘.

[[라즈베리에서:
    cd /home/dzp/dzp-main/program/investment-advisor
    git pull
    sudo systemctl restart investment-advisor-api    # lazy 패치된 코드로 재기동
    # 이후 06:30 배치들은 자기 세션을 자유롭게 만들 수 있음

[추가4] 후속작업 진행해줘.]

/commit

========================



========================
========================
========================
========================

"Phase C 후보 (자연어 입력 / 비교 모드 / 필터 알림)" 도 정리해서 알려줘.
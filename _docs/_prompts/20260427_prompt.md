========================
2026.04.27(월)
========================

[[라즈베리파이 배포:
    cd /home/dzp/dzp-main/program/investment-advisor
    source venv/bin/activate
    git pull origin main
    sudo systemctl restart investment-advisor-api

========================

운영기 배포 (사용자 수동 작업)

cd /home/dzp/dzp-main/program/investment-advisor
git pull
source venv/bin/activate
python -c "from shared.db import init_db; from shared.config import DatabaseConfig; init_db(DatabaseConfig())"   # v39, v40 자동 적용
sudo cp deploy/systemd/investment-advisor-fundamentals.{service,timer} /etc/systemd/system/
# /etc/sudoers.d/investment-advisor-systemd 에 README 의 신규 sudoers 행 5개 추가
sudo visudo -c -f /etc/sudoers.d/investment-advisor-systemd
sudo systemctl daemon-reload
sudo systemctl enable --now investment-advisor-fundamentals.timer
sudo systemctl start investment-advisor-fundamentals.service   # 첫 sync 수동 실행
journalctl -u investment-advisor-fundamentals.service -f -n 100


[추가1] 현재 작업 된 서비스 가 모두, D:\_dzp\VIBE_CODING\investment-advisor\deploy 에 정리되어 있고, 웹페이지의 "관리자 메뉴"에서 제어 가능한지 확인해서 알려줘.

========================

/brainstorming 혹시 지금까지의 구현 부분에서 일반적인 주식분석 프로그램 대비 부족한 부분이나 클로드 ai를 추가활용 가능한 방안을 제안해줘.

[추가1] (e) 일단 다 보고 결정 
[추가2] (1) Sprint 1 전체 의 design doc을 쓴다 (5개 항목 통합 spec)
[추가2-1]
1.Vision 무료티어 : anonymous 1회 체험 허용
2.글로벌 뉴스 region별 카테고리 : OK
3.NL→SQL read-only DB user : OK
4.Red Team 기본 OFF : OK
5.거장 시드 5개 : OK

[추가3] 제안해준대로 계속 진행해줘.
[추가4] "(1) Subagent-Driven (권장)" 방법으로 진행해줘.
[추가5] PR-2 진행해줘

========================
========================
========================
========================

========================

[Sprint 1 PR-1 배포 — 운영기 명령]

cd /home/dzp/dzp-main/program/investment-advisor
git pull
source venv/bin/activate
python -c "from shared.db import init_db; from shared.config import AppConfig; init_db(AppConfig().db)"  # v41 자동 적용
sudo systemctl restart investment-advisor-api

# 검증
psql -d investment_advisor -c "SELECT version FROM schema_version ORDER BY version DESC LIMIT 1;"  # → 41
psql -d investment_advisor -c "SELECT COUNT(*) FROM screener_presets WHERE is_seed=TRUE;"          # → 10
psql -d investment_advisor -c "\d nl_search_history"
psql -d investment_advisor -c "\d chart_vision_log"
psql -d investment_advisor -c "\d krx_investor_flow_daily"
psql -d investment_advisor -c "\d stock_universe_factor_snapshot"

========================

[Sprint 1 PR-2 배포 — 운영기 명령]

cd /home/dzp/dzp-main/program/investment-advisor
git pull
sudo systemctl restart investment-advisor-api  # API 라우트는 영향 없음 — 안전 재기동

# 다음 분석 실행 시 자동으로 글로벌 뉴스 수집 + region-grouped Stage 1 입력 사용
# 글로벌 뉴스 끄려면 .env 에 GLOBAL_NEWS_ENABLED=false 후 service restart

# 검증 — 신규 region 컬럼이 채워지는지 (분석 1회 실행 후)
sudo -u postgres psql -d investment_advisor -c \
  "SELECT region, lang, COUNT(*) FROM news_articles
   WHERE created_at >= NOW() - INTERVAL '24 hours'
   GROUP BY region, lang ORDER BY 3 DESC;"

========================
========================
2026.04.28(화)
========================

[[라즈베리파이 배포:
    cd /home/dzp/dzp-main/program/investment-advisor
    source venv/bin/activate
    git pull origin main
    sudo systemctl restart investment-advisor-api

========================

cd /home/dzp/dzp-main/program/investment-advisor
git pull                # 새 .env.example 받기
cp .env .env.bak        # 현재 .env 백업
cp .env.example .env    # .env.example 을 새 .env 로 복사

# 백업본 보면서 비밀번호·API 키만 새 .env 에 옮겨 쓴다
diff .env.bak .env
nano .env               # 비밀번호 등 수정

sudo systemctl restart investment-advisor-api
sudo systemctl start investment-advisor-analyzer.service
journalctl -u investment-advisor-analyzer.service -f -n 30

========================

첨부이미지와 같이 우측상단에 사용자정보 등의 메뉴가 없어지고, 왼쪽 하단에 예전의 방식인 관리자 메뉴, api docs 가 표시되고 있어. 확인해줘.

========================

대화를 "Thema Chat", "AI Tutor"와 같이 어떤 주제를 선택하고 질문하는거 말고, 그냥 일반적인 질문을 하는 기능을 추가하고 싶어. 구성을 어떻게 진행하는게 좋을지 제안해줘.

[추가1] 제안한 방법으로 작업 진행해줘.

/commit

========================
========================
========================
========================
========================
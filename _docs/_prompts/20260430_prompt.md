========================
2026.04.30(목)
========================

[[라즈베리파이 배포:
    cd /home/dzp/dzp-main/program/investment-advisor
    source venv/bin/activate
    git pull origin main
    sudo systemctl restart investment-advisor-api

========================

[[외국인 수급 스크리너 (Task 8 UI):
    Task 8 — screener.html 외국인 수급 사이드패널 그룹 + SpecBuilder + chips + 결과 컬럼

    - 사이드패널 그룹 7번 "외국인 수급" 추가 (data-group="foreign")
      - 현재 보유율 ≥ 입력 (f-foreign-own-min)
      - 변화 윈도우 라디오 (5/20/60일, 기본 20일)
      - 보유율 변화 ≥ 입력 (f-foreign-delta-min)
      - 순매수 윈도우 라디오 (5/20/60일, 기본 20일)
      - 누적 순매수 ≥ 입력 (f-foreign-netbuy-min, 단위: 억원)
    - SpecBuilder.fromDOM / toDOM 5개 키 양방향 매핑
      - min_foreign_ownership_pct, min_foreign_ownership_delta_pp,
        min_foreign_net_buy_krw (억→원 *1e8 변환), delta_window_days, net_buy_window_days
    - CHIP_DEFS 3개 추가 (chip-foreign 카테고리, 오렌지 테두리)
    - GROUP_SPEC_KEYS / GROUP_TO_CATS 에 'foreign' 그룹 등록
    - ColumnDefs 3개 추가 (foreign_own, foreign_delta, foreign_netbuy)
    - ViewColumns 에 'foreign' 뷰 추가 (버튼: "외국인")
    - f-sort select 에 3개 옵션 추가 (foreign_ownership_desc / foreign_delta_desc / foreign_net_buy_desc)
    - renderActiveChips 에서 def.label(v, spec) 으로 spec 전달 수정
    - 기존 테스트 24/24 통과
    - Step 5 수동 UI 검증은 Task 12 (운영기 배포) 로 defer
]]

[[systemd foreign-flow-sync unit 파일명 prefix 수정:
    Critical fix — 파일명과 참조 이름 불일치 해결

    - deploy/systemd/foreign-flow-sync.service → investment-advisor-foreign-flow-sync.service (git mv)
    - deploy/systemd/foreign-flow-sync.timer → investment-advisor-foreign-flow-sync.timer (git mv)
    - CLAUDE.md DB Schema 섹션 foreign-flow-sync.timer 언급에 prefix 추가
    - admin_systemd.py MANAGED_UNITS / README.md sudoers 가이드는 이미 prefix 있는 이름 참조 중 — 변경 불필요
    - 결정 근거: admin_systemd.py 가 investment-advisor-foreign-flow-sync.{service,timer} 로 참조 → 파일명을 참조에 맞춰 rename (옵션 A)
    - 테스트: admin_systemd 관련 35 PASS 확인
]]


========================

"Merge stock history and basic info pages" 이어서 작업해줘.

[작업 결과]
옛 ticker_history.html 페이지를 stock_cockpit.html (cockpit) 으로 흡수, 라우트/템플릿 삭제.

- /api/stocks/{ticker}/proposals 응답 확장:
    target_allocation / current_price / upside_pct / quant_score / sentiment_score / currency / market 7개 필드 추가.
- stock_cockpit.html § 6 (AI 추천 이력) 헤더에 요약 라인 (#history-summary)
    — 최초 추천 / 최근 추천 / 추천 횟수 / 등장 테마 수.
- stock_cockpit.html § 6-B 신규 — 일자별 상세 표 (#sec-history-table / #history-table-body)
    날짜·Action·확신도·비중·현재가·목표가·상승여력·퀀트·센티먼트·테마 10컬럼.
- stock_cockpit.html header_actions 재구성:
    구독 버튼(로그인 필수) + external_links(아이콘) + 제안 목록 — 옛 "추천 이력" 링크 제거.
- stock_cockpit.js 에 renderHistorySummary / renderHistoryTable 추가, getProposals 의 단일 fetch 결과를 3 곳 (요약/타임라인/표) 동시 렌더.
- /pages/proposals/history/{ticker} 라우트 (proposals.py:ticker_history_page) + ticker_history.html 템플릿 삭제.
- 16 개 소스 파일에서 /pages/proposals/history/{ticker} → /pages/stocks/{ticker} 일괄 치환:
    api/templates/{screener,themes,watchlist,proposals,_macros/proposal,theme_history,session_detail,stock_analysis}.html,
    api/templates/partials/dashboard/{_top_picks,_signals_today,_themes_list,_hero_row2}.html,
    analyzer/signals.py, shared/db/session_repo.py, scripts/route_baseline.py, README.md.
- CLAUDE.md templates 섹션 갱신 — ticker_history 제거 + cockpit 흡수 명시. README.md 페이지 표 갱신.
- 테스트: tests/test_stock_cockpit.py 32 → 34 PASS (신규 2: history-table+summary 마크업, JS renderer 시그니처).
    전체 회귀 — 변경 전/후 동일한 14 fail / 6 error (모두 pre-existing CKPT-4/5 류 + admin_tier_audit / news_collector_global / optimizations).

[추가1] 어, commit 하고, push 까지해줘.

========================

Screener 페이지의 상단의 "빠른 시작" 카드를 접을 수 있도록 해주고, 초기값을 접혀서 표시되도록 수정해줘.

========================

Screener 페이지의 조회조건이나 UX가 조작도 불편하고, 보기에도 가독성이 안좋아. 개선할수 있는 방안을 정리해서 알려줘. 다른 참조할 사이트나 서비스가 있으면 직접 참고하고, 제안해줘.

[추가1] 문서는 모두 md 형식으로 생성해줘.
[추가2] 1.Phase A 전체 (7개) 진행 해줘

[작업 결과]
Phase A 7개 모두 적용 — 단일 commit (UI/UX 통합 패치).

- A6: 외국인 그룹 KRX 의존성 자동 disable
    `data-requires-market="KOSPI,KOSDAQ"` 메타 → updateMarketDependentGroups()
    시장에 NASDAQ/NYSE 만 선택 시 외국인 그룹 body opacity 0.45 + pointer-events:none + 안내 배너.
- A5: 정렬 select 위치 이동
    "기술·수익률" 그룹 안에 묻혀있던 16옵션 select 를 결과 영역 헤더로 이동.
    SpecBuilder/CHIP_DEFS 변경 없음 — id="f-sort" 동일 유지.
- A4: Group 7 → 4 통합 (localStorage v3)
    검색+시장+시총 → 🎯 기본 / 수익률+기술 → 📈 기술·수익률 / 펀더 / 수급
    GROUPS_KEY = 'screener_groups_open_v3' (v2 cache invalidate),
    DEFAULT_OPEN = ['basic'], GROUP_TO_CATS / GROUP_SPEC_KEYS 4키로 재맵.
- A7: URL state 영속화
    `?spec=<base64url-of-json>` 으로 spec 인코딩 → 새로고침/공유/back 동작.
    encodeSpec/decodeSpec 헬퍼, history.replaceState(),
    구버전 평면 파라미터(?q/?sectors/?markets) 호환 유지.
- A2: Preset bin 빠른 선택 칩
    각 입력 옆 .preset-bins 그룹 — PER ≤15/≤20/≤25, PBR ≤1.0/≤1.5/≤2.0,
    배당 ≥2/3/4%, 변동성 보수 2.5/균형 3.0/적극 4.0, 거래량 ×1.2/1.5/2.0,
    52w근접 신고가/15%/25%, 낙폭 ≤10/20/30%.
    클릭 = 값 set + toggle (같은 값이면 비움) + auto-run.
- A3: /api/screener/count 경량 엔드포인트
    routes/screener.py — _build_screener_filters() 추출 (run/count 공유),
    /count 가 SELECT COUNT(*) only (sparkline_60d ARRAY_AGG / row enrichment 없음).
    UI: 사이드패널 footer #liveCountBadge — debounce 300ms,
    AbortController 로 in-flight 취소, 매칭 0/>500 색상 강조.
- A1: Chips bar 편집형 popover
    CHIP_DEFS 각 항목에 editor 메타 (type: text/number/range, input id, scale, suffix),
    chip 클릭 → openChipEditor() 가 anchor 위치 popover 띄움 (number / text / range),
    Enter = 적용, Esc/외부 클릭 = 취소. × 버튼은 stopPropagation.
- 신규 테스트 4개 추가 (test_screener_run.py 의 _override_db 에 fetchone 지원,
  /count smoke + ohlcv CTE + foreign CTE + 0건 케이스). 27 → 31 PASS.
  전체 회귀 — pre-existing 14 fail / 6 error 동일 (CKPT-4/5 류, 무관).

========================

공공데이터 중에 수출입 데이터를 수집하고, 이 정보를 가지고 인사이트를 찾아서 종목을 추천하는 로직을 추가하는 부분이 구현을 하면 투자결정에 인사이트를 찾아 줄수 있을지 검토해줘.

========================
========================
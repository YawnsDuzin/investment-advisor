========================
2026.04.30(목)
========================

[[외국인 수급 스크리너 (Task 8 UI):
    Task 8 — screener.html 외국인 수급 사이드패널 그룹 + SpecBuilder + chips + 결과 컬럼

    - 사이드패널 그룹 7번 "외국인 수급" 추가 (data-group="foreign")
      - 현재 보유율 ≥ 입력 (f-foreign-own-min)
      - 변화 윈도우 라디오 (5/20/60일, 기본 20일)
      - 보유율 변화 ≥ 입력 (f-foreign-delta-min)
      - 순매수 윈도우 라디오 (5/20/60일, 기본 20일)
      - 누적 순매수 ≥ 입력 (f-foreign-netbuy-min, 단위: 억원)
    - SpecBuilder.fromDOM / toDOM 5개 키 양방향 매핑
      - min_foreign_ownership_pct, min_foreign_ownership_delta_pp,
        min_foreign_net_buy_krw (억→원 *1e8 변환), delta_window_days, net_buy_window_days
    - CHIP_DEFS 3개 추가 (chip-foreign 카테고리, 오렌지 테두리)
    - GROUP_SPEC_KEYS / GROUP_TO_CATS 에 'foreign' 그룹 등록
    - ColumnDefs 3개 추가 (foreign_own, foreign_delta, foreign_netbuy)
    - ViewColumns 에 'foreign' 뷰 추가 (버튼: "외국인")
    - f-sort select 에 3개 옵션 추가 (foreign_ownership_desc / foreign_delta_desc / foreign_net_buy_desc)
    - renderActiveChips 에서 def.label(v, spec) 으로 spec 전달 수정
    - 기존 테스트 24/24 통과
    - Step 5 수동 UI 검증은 Task 12 (운영기 배포) 로 defer
]]
